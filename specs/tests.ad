# Tests

> Executable verification criteria for the Agents Relay module.


## Tests

---

## S1.1 [G1] Agent profession registry loads built-in professions
**Status:** done
**Test File:** `crates/auto-forge/src/relay/profession.rs` (inline `#[cfg(test)]`)
**Depends on:** G1, D1

```rust
let registry = ProfessionRegistry::new();
```

1. Call `registry.load_builtin()`
2. Query `registry.get("architect")`
3. Query `registry.get("coder")`
4. Query `registry.get("nonexistent")`

- Architect profession exists with `owned_sections: [Architecture, Designs]`.
- Coder profession exists with `tools: [read_file, write_file, edit_file, shell, search]`.
- Nonexistent returns `None`.



---

## S1.2 [G1] Soul markdown renders into system prompt context
**Status:** done
**Test File:** `crates/auto-forge/src/relay/agent.rs` (inline `#[cfg(test)]`)
**Depends on:** G1, D1

```rust
let soul = SoulConfig::load("test-souls/planner.md").unwrap();
let profession = ProfessionRegistry::new().get("planner").unwrap();
```

1. Call `AgentInstance::spawn(profession, &soul.id, ...)`
2. Call `agent.render_system_prompt()`
3. Assert prompt contains Soul values
4. Assert prompt contains Profession constraints

- System prompt includes "Core Values" section from Soul.
- System prompt includes "You may ONLY use read_jade, write_jade tools" from Profession.
- Prompt is ≤2000 tokens.



---

## S2.1 [G2] PipelineEngine executes flow steps in order
**Status:** done
**Test File:** `crates/auto-forge/src/relay/pipeline.rs` (inline `#[cfg(test)]`)
**Depends on:** G2, D2

```rust
let flow = FlowSpec::parse(r#"
    $Step(profession: "planner", gate: "auto")
    $Step(profession: "architect", gate: "auto")
    $Step(profession: "coder", gate: "auto")
"#).unwrap();
let mut engine = PipelineEngine::new(flow, "test-run-1".into());
```

1. Mock all agent turns to return immediate handoff.
2. Call `engine.execute_next()` three times.
3. Record profession of each spawned agent.

- Step 1 spawns Planner.
- Step 2 spawns Architect.
- Step 3 spawns Coder.
- No step is skipped or reordered.



---

## S2.2 [G2] Flow branching routes DIRECT intent to single Coder step
**Status:** done
**Test File:** `crates/auto-forge/src/relay/pipeline.rs` (inline `#[cfg(test)]`)
**Depends on:** G2, D2

```rust
let flow = FlowSpec::parse(r#"
    $Step(profession: "intaker", gate: "auto") {
      exit: "branch(intent)"
    }
    $Step(profession: "planner", gate: "auto")
    $Step(profession: "coder", gate: "auto")
"#).unwrap();
let mut engine = PipelineEngine::new(flow, "test-run-2".into());
```

1. Mock Intaker to classify intent as `DIRECT`.
2. Call `engine.execute_next()`.
3. Observe which profession runs next.

- Intaker classifies `DIRECT`.
- Flow branches to Coder step, skipping Planner.
- Total steps executed: 2 (Intaker + Coder).



---

## S3.1 [G3] Handoff document excludes raw chat history
**Status:** done
**Test File:** `crates/auto-forge/src/relay/handoff.rs` (inline `#[cfg(test)]`)
**Depends on:** G3, D3

```rust
let agent = create_test_agent("coder");
let history = vec![
    TurnRecord::user("Implement auth"),
    TurnRecord::assistant("I'll start by..."),
    TurnRecord::tool_call("read_file", json!({"path": "src/main.rs"})),
    TurnRecord::tool_result("file contents..."),
];
```

1. Call `HandoffDocument::generate(&agent, &history, &specs)`.
2. Inspect rendered handoff markdown.
3. Search for raw chat phrases.

- Handoff contains summary like "Coder implemented auth module."
- Handoff does NOT contain "I'll start by..." or raw file contents.
- Handoff lists `work_product: ["src/auth.rs"]` instead of inlining code.



---

## S3.2 [G3] Context scope filters specs by profession
**Status:** done
**Test File:** `crates/auto-forge/src/relay/handoff.rs` (inline `#[cfg(test)]`)
**Depends on:** G3, D3

```rust
let specs = load_test_specs(); // Contains Goals, Architecture, Designs, Plans, Tests
let coder_profession = ProfessionRegistry::new().get("coder").unwrap();
```

1. Call `ContextPointers::build(&coder_profession, &specs)`.
2. Count sections included in rendered context.

- Context includes Plans and Designs (readable by Coder).
- Context includes Tests (readable by Coder).
- Context excludes Architecture (not in Coder's readable_sections).
- Context excludes Reviews (not in Coder's readable_sections).



---

## S4.1 [G4] Checkpoint restores after simulated crash
**Status:** done
**Test File:** `crates/auto-forge/tests/checkpoint_recovery.rs`
**Depends on:** G4, D4

```rust
let temp_dir = TempDir::new().unwrap();
let project_dir = temp_dir.path().join("project");
// Create test files and ledger
```

1. Run PipelineEngine through 3 steps.
2. Capture checkpoint after step 2.
3. Simulate crash: drop engine, delete in-memory state.
4. Create new engine from checkpoint.
5. Resume execution.

- New engine resumes at step 3 (Architect handoff).
- File system state matches checkpoint (files at step 2 state).
- Ledger state matches checkpoint.
- No chat history from step 1 or 2 is present.



---

## S4.2 [G4] Resume rehydrates agent without chat history
**Status:** done
**Test File:** `crates/auto-forge/src/relay/checkpoint.rs` (inline `#[cfg(test)]`)
**Depends on:** G4, D4

```rust
let checkpoint = load_test_checkpoint("step-2-checkpoint.json");
```

1. Call `PipelineEngine::resume(&checkpoint)`.
2. Inspect the spawned agent's context.
3. Check `agent.context.messages`.

- Agent context contains Soul + relevant Specs + Handoff Document.
- Agent context does NOT contain raw messages from Planner or Architect turns.
- `agent.context.turns_taken` is 0 (fresh start).



---

## S5.1 [G5] Token budget hard-stops at limit
**Status:** done
**Test File:** `crates/auto-forge/src/relay/budget.rs` (inline `#[cfg(test)]`)
**Depends on:** G5, D5

```rust
let budget = TokenBudget {
    limit: 1000,
    warning_at: 700,
    strategy: BudgetStrategy::HardStop,
};
let mut tracker = BudgetTracker::new(budget);
```

1. Record 600 tokens → check action.
2. Record 300 more tokens (total 900) → check action.
3. Record 200 more tokens (total 1100) → check action.

- At 600: `BudgetAction::None`.
- At 900: `BudgetAction::Warning { remaining: 100 }`.
- At 1100: `BudgetAction::HardStop`.



---

## S6.1 [G6] Human gate pauses until approval received
**Status:** done
**Test File:** `crates/auto-forge/tests/human_gate.rs`
**Depends on:** G6, D2

```rust
let flow = FlowSpec::parse(r#"
    $Step(profession: "planner", gate: "human")
    $Step(profession: "architect", gate: "auto")
"#).unwrap();
let mut engine = PipelineEngine::new(flow, "test-run-gate".into());
```

1. Mock Planner turn to return handoff.
2. Call `engine.execute_next()`.
3. Assert status is `WaitingForHuman`.
4. Wait 100ms (simulate human delay).
5. Call `engine.resolve_gate(GateDecision::Approve)`.
6. Assert status transitions to `Running` with Architect.

- After Planner step: `PipelineStatus::WaitingForHuman`.
- Before approval: no Architect spawned.
- After approval: Architect step begins.
- After rejection: Planner step restarts with feedback.



---

## S6.2 [G6] End-to-end standard flow with human gates and budget tracking
**Status:** done
**Test File:** `crates/auto-forge/tests/relay_integration.rs`
**Depends on:** G1-G6, P6

```rust
let flow = standard_spec_flow(); // 6 steps: Intaker → Planner → Architect → Coder → Tester → Reviewer
let store = new_run_store();
```

1. Start run with standard flow.
2. Execute Intaker step, submit handoff.
3. Approve Planner gate, execute Planner, submit handoff.
4. Approve Architect gate, execute Architect, submit handoff.
5. Execute Coder, Tester, Reviewer steps with handoffs.
6. Verify final status is Completed.

- All 6 steps complete in order.
- Budget tracks cumulative tokens across all steps.
- Savings vs parallel estimate is positive.



---

## S6.3 [G6] Gate rejection routes back to same profession with feedback
**Status:** done
**Test File:** `crates/auto-forge/tests/relay_integration.rs`
**Depends on:** G6, D2

```rust
let flow = standard_spec_flow();
let store = new_run_store();
```

1. Execute Intaker step, submit handoff.
2. Reject Planner gate with feedback.
3. Verify engine re-enters Planner step.

- After rejection, current_step remains on Planner.
- Feedback is stored in engine.gate_feedback.



---

## S4.3 [G4] Checkpoint created during flow and resumed
**Status:** done
**Test File:** `crates/auto-forge/tests/relay_integration.rs`
**Depends on:** G4, D4

```rust
let flow = standard_spec_flow();
let store = new_run_store();
```

1. Run through 3 steps of standard flow.
2. Create Checkpoint from engine state.
3. Drop original engine.
4. Resume from checkpoint with PipelineEngine::from_checkpoint().
5. Continue execution from step 4.

- Checkpoint captures current_step = 3, step_history len = 3.
- Resumed engine has Idle status, no chat history.
- Can advance to Coder step.

---

## S7.1 [G7] Profession registry contains new v2 professions and no Intaker or APIs
**Status:** done
**Test File:** `crates/auto-forge/src/relay/profession.rs` (inline `#[cfg(test)]`)
**Depends on:** G7, D7, P7.1, P7.2

```rust
let registry = ProfessionRegistry::new();
```

1. Query `registry.get("assistant")`.
2. Query `registry.get("advisor")`.
3. Query `registry.get("intaker")`.
4. Query `registry.get("architect")`.
5. Inspect `architect.owned_sections`.

- Assistant exists with `phase: Intake`, `owned_sections: []`.
- Advisor exists with `phase: Discovery`, `owned_sections: [Goals]`.
- Intaker returns `None`.
- Architect exists with `owned_sections: [Architecture, Designs]` (no APIs).

---

## S7.2 [G6] GSD mode auto-approves all gates except GoalGate
**Status:** done
**Test File:** `crates/auto-forge/src/relay/pipeline.rs` (inline `#[cfg(test)]`)
**Depends on:** G6, D6, P7.3, P7.5

```rust
let flow = gsd_flow(); // Advisor (human gate) → Architect (auto) → Planner (auto) → ...
let mut engine = PipelineEngine::new(flow, "test-gsd".into());
engine.mode = RelayMode::GSD;
```

1. Execute Advisor step.
2. Assert status is `WaitingForHuman` (GoalGate).
3. Approve GoalGate.
4. Execute Architect step.
5. Assert status is `Running` (NOT WaitingForHuman).
6. Execute Planner, Coder, Tester, Reviewer, Documenter.

- Only GoalGate pauses in GSD mode.
- All other gates proceed automatically.
- Final status is `Completed` without further human interaction.

---

## S7.3 [G6] Check mode pauses at every human-configured gate
**Status:** done
**Test File:** `crates/auto-forge/src/relay/pipeline.rs` (inline `#[cfg(test)]`)
**Depends on:** G6, D6, P7.3, P7.5

```rust
let flow = check_flow(); // Advisor (human) → Architect (human) → Planner (human) → ...
let mut engine = PipelineEngine::new(flow, "test-check".into());
engine.mode = RelayMode::Check;
```

1. Execute Advisor step → assert `WaitingForHuman`.
2. Approve → execute Architect → assert `WaitingForHuman`.
3. Approve → execute Planner → assert `WaitingForHuman`.
4. Approve → execute remaining steps.

- Every gate with `gate: human` pauses the engine.
- All 3 human gates are exercised.

---

## S7.4 [G7] New end-to-end flow executes in correct order
**Status:** done
**Test File:** `crates/auto-forge/tests/relay_integration.rs`
**Depends on:** G1-G7, P7

```rust
let flow = new_v2_flow(); // Assistant → Advisor → Architect → Planner → Tester → Coder → Tester → Reviewer → Documenter
let store = new_run_store();
```

1. Start run in GSD mode.
2. Execute all steps with mocked handoffs.
3. Record profession order.

- Step 1: Assistant.
- Step 2: Advisor.
- Step 3: Architect.
- Step 4: Planner.
- Step 5: Tester (drafts tests).
- Step 6: Coder.
- Step 7: Tester (runs tests).
- Step 8: Reviewer.
- Step 9: Documenter.
- Total 9 steps in order.


---

## S8.1 [G8] GateCard approval flow — approve triggers pipeline advance
**Status:** done
**Test File:** `packages/auto-forge-ui/tests/integration/GateCard.spec.ts`
**Depends on:** G8, D8

```typescript
const gate = createPendingGate({ gateType: 'goal', profession: 'advisor' })
render(<GateCard gate={gate} mode="inline" />)
```

1. Click "Approve & Execute" button
2. Assert `approveSpec()` API is called
3. Assert gate card transitions to `approved` state
4. Assert `streamResponse()` resumes




---

## S8.2 [G8] SecretaryMessage queue — multiple gates enqueue and dequeue correctly
**Status:** done
**Test File:** `packages/auto-forge-ui/tests/integration/SecretaryMessage.spec.ts`
**Depends on:** G8, D9

```typescript
const inbox = useGateInbox()
inbox.registerGate(gateA)  // becomes active
inbox.registerGate(gateB)  // queued
inbox.registerGate(gateC)  // queued
```

1. Render `<SecretaryMessage gate={inbox.active} />`
2. Assert "+2 more" pill is visible
3. Click pill; assert mini-list shows gateB and gateC
4. Resolve gateA; assert gateB becomes active




---

## S8.3 [G8] useGateInbox snooze — snoozed gate reappears after timeout
**Status:** done
**Test File:** `packages/auto-forge-ui/tests/unit/useGateInbox.spec.ts`
**Depends on:** G8, D9

```typescript
const inbox = useGateInbox()
inbox.registerGate(gateA)
```

1. Call `inbox.snoozeGate(gateA.id, 0.1)` (100ms for test)
2. Assert active secretary is null
3. Wait 150ms
4. Assert gateA is back as active




---

## S9.1 [G9] Category renderers switch correctly per section_type
**Status:** done
**Test File:** `packages/auto-forge-ui/tests/integration/CategoryRenderers.spec.ts`
**Depends on:** G9, D10

1. Render `<SpecsView />`
2. Click each sidebar section
3. Assert correct renderer component mounts
4. Assert `GoalsTable` renders table; `ArchitectureCards` renders cards




---

## S9.2 [G9] RelationsPanel resolves parents and children across sections
**Status:** done
**Test File:** `packages/auto-forge-ui/tests/integration/RelationsPanel.spec.ts`
**Depends on:** G9, D11

1. Select G1 in GoalsTable
2. Assert RelationsPanel shows "Children: A1, D1, S1.1"
3. Click A1; assert section switches to Architecture, A1 expands
4. Assert A1 RelationsPanel shows "Parents: G1"




---

## S9.3 [G9] AutoLinkContent parses IDs and emits jump events
**Status:** done
**Test File:** `packages/auto-forge-ui/tests/unit/AutoLinkContent.spec.ts`
**Depends on:** G9, D11

1. Render `<AutoLinkContent content={content} />`
2. Assert 3 `<a class="spec-link">` elements rendered
3. Click G1 link; assert `jump(id)` event emitted with `"G1"`




---

## S10.1 [G10] Node graph renders 9-step linear pipeline correctly
**Status:** done
**Test File:** `packages/auto-forge-ui/tests/integration/PipelinePanel.spec.ts`
**Depends on:** G10, D12

1. Render `<PipelinePanel nodes={nodes} edges={edges} />`
2. Assert 9 dots are visible
3. Assert 3 green, 1 amber pulse, 5 gray
4. Assert 8 connecting lines between dots




---

## S10.2 [G10] Expanded node card shows correct token burn and specs touched
**Status:** done
**Test File:** `packages/auto-forge-ui/tests/integration/NodeCard.spec.ts`
**Depends on:** G10, D13

1. Click Architect dot in PipelinePanel
2. Assert expanded card shows "8,100 tokens"
3. Assert card shows "architecture, designs (2 writes)"
4. Click "View written specs →"; assert Specs drawer opens




---

## S10.3 [G10] Retry loop backward edge renders and pulses on test failure
**Status:** done
**Test File:** `packages/auto-forge-ui/tests/integration/PipelinePanel.retry.spec.ts`
**Depends on:** G10, D12

1. Render PipelinePanel with retry edge
2. Assert backward arrow is visible between Tester and Coder
3. Assert arrow pulses amber
4. Assert Coder node shows "Retry 2/3" badge




---

## S10.4 [G11] Mobile layout collapses Zone C to bottom sheet
**Status:** done
**Test File:** `packages/auto-forge-ui/tests/e2e/mobile-layout.spec.ts`
**Depends on:** G10, D14

1. Open Specs view
2. Select an item with relations
3. Assert RelationsPanel renders as bottom sheet, not sidebar
4. Swipe down; assert sheet dismisses


---

## S13.1 [G13] ApiSource CRUD operations persist and load correctly
**Status:** proposed
**Test File:** `backend/src/relay/config.rs` (inline `#[cfg(test)]`)
**Depends on:** G13, D19

```rust
let temp_dir = TempDir::new().unwrap();
let config_dir = temp_dir.path().join("autoforge");
```

1. Create `ApiSource { id: "test-anthropic", provider: Anthropic, ... }`
2. Call `save_api_sources(&[source])`
3. Call `load_api_sources()`
4. Assert loaded source matches original
5. Update source name, call `save_api_sources()`
6. Reload, assert name updated
7. Delete by saving empty array, reload, assert empty

- Save and load round-trips without data loss.
- Multiple sources can be persisted.
- Empty state loads as empty Vec.


---

## S13.2 [G13] Auto-detection discovers Anthropic key from ~/.claude/settings.json
**Status:** proposed
**Test File:** `backend/src/relay/config.rs` (inline `#[cfg(test)]`)
**Depends on:** G13, D19

```rust
// Set up mock ~/.claude/settings.json with ANTHROPIC_AUTH_TOKEN
let temp_home = TempDir::new().unwrap();
```

1. Write `settings.json` with `env.ANTHROPIC_AUTH_TOKEN = "sk-test-key"`
2. Call `detect_available_providers()`
3. Assert one Anthropic source returned
4. Assert source has 3 models (haiku, sonnet, opus) with correct tiers
5. Assert `is_available == true`

- Anthropic source detected with correct models.
- All three tiers (Light/Mid/Heavy) populated.
- Source is marked available.


---

## S13.3 [G13] API key resolution follows priority order
**Status:** proposed
**Test File:** `backend/src/relay/config.rs` (inline `#[cfg(test)]`)
**Depends on:** G13, D19

```rust
let source = ApiSource {
    api_key_env: "TEST_API_KEY".into(),
    api_key_stored: Some(base64_encode("stored-key")),
    ...
};
```

1. Set env var `TEST_API_KEY=env-key`; call `resolve_api_key()` → assert "env-key"
2. Unset env var; call `resolve_api_key()` → assert "stored-key" (decoded from base64)
3. Set `api_key_env = "settings:ANTHROPIC_API_KEY"` with mock settings → assert settings value
4. Remove all sources → assert `None`

- Env var takes priority over stored key.
- Settings path works as fallback.
- Returns None when no key is available.


---

## S13.4 [G13] Multi-provider dispatch routes to correct API format
**Status:** proposed
**Test File:** `backend/src/ai.rs` (inline `#[cfg(test)]`)
**Depends on:** G13, D20

```rust
// Mock HTTP responses for each provider
let anthropic_source = ApiSource { provider: Anthropic, ... };
let openai_source = ApiSource { provider: OpenAI, ... };
```

1. Call `dispatch_chat(&anthropic_source, "claude-3-5-sonnet", ...)` with mock server
2. Assert request uses `x-api-key` header, `/v1/messages` path
3. Assert SSE parsing extracts `delta.text` from `content_block_delta`
4. Call `dispatch_chat(&openai_source, "gpt-4o", ...)` with mock server
5. Assert request uses `Authorization: Bearer` header, `/v1/chat/completions` path
6. Assert SSE parsing extracts `choices[0].delta.content`

- Anthropic provider sends correct auth header and URL.
- OpenAI provider sends correct auth header and URL.
- Both SSE formats are parsed into `AIStreamDelta` correctly.


---

## S13.5 [G13] Test connection endpoint validates API credentials
**Status:** proposed
**Test File:** `backend/src/relay/api.rs` (integration test)
**Depends on:** G13, D19

```rust
// POST /api/forge/config/api-sources/{id}/test
```

1. Create source with valid Anthropic key
2. Call test connection endpoint
3. Assert response `{ "success": true, "model": "claude-3-5-sonnet" }`
4. Create source with invalid key
5. Call test connection endpoint
6. Assert response `{ "success": false, "error": "Authentication failed" }`

- Valid credentials return success with model confirmation.
- Invalid credentials return descriptive error.
- Local provider checks connectivity to URL.

---

## S14.1 [G14] ClaudeProvider loads API config from ~/.claude/settings.json or env vars
**Status:** done
**Test File:** `backend/src/provider/claude.rs` (inline `#[cfg(test)]`)
**Depends on:** G14, D21

1. Set `ANTHROPIC_API_KEY` env var
2. Call `ClaudeProvider::new()`
3. Assert `is_available() == true`
4. Unset env var, create mock `~/.claude/settings.json` with `ANTHROPIC_AUTH_TOKEN`
5. Call `ClaudeProvider::new()`
6. Assert `is_available() == true`

- Provider detects key from env var.
- Provider detects key from settings.json.
- Provider returns `false` when no key is available.

---

## S14.2 [G14] ToolError enum serializes and displays correctly
**Status:** done
**Test File:** `backend/src/forge/tools.rs` (inline `#[cfg(test)]`)
**Depends on:** G14, D21

1. Create `ToolError::ExecutionFailed("test error".into())`
2. Assert `to_string()` contains "test error"
3. Create `ToolError::InvalidInput("bad input".into())`
4. Assert `to_string()` contains "bad input"
5. Create `ToolError::PermissionDenied("denied".into())`
6. Assert `to_string()` contains "denied"

- Each variant displays its message.
- `Display` trait works for error formatting.

---

## S14.3 [G14] Read-only tools correctly report is_read_only()
**Status:** done
**Test File:** `backend/src/forge/tools.rs` (inline `#[cfg(test)]`)
**Depends on:** G14, D21

1. Create `ReadFileTool`, `SearchTool`, `ReadJadeTool`, `ListJadesTool`
2. Assert all return `is_read_only() == true`
3. Create `WriteFileTool`, `EditFileTool`, `ShellTool`, `WriteJadeTool`
4. Assert all return `is_read_only() == false`

- File-reading tools are classified as read-only.
- Writing and shell tools are classified as read-write.

---

## S14.4 [G14] ContextManager compacts messages when approaching token limit
**Status:** done
**Test File:** `backend/src/runtime/context.rs` (inline `#[cfg(test)]`)
**Depends on:** G14, D23

1. Create `ContextManager` with low `max_tokens` (100)
2. Build messages exceeding threshold
3. Call `maybe_compact(&mut messages)`
4. Assert returns `true` and messages were compacted
5. Call `maybe_compact` again with compacted messages
6. Assert returns `false`

- Compaction triggers at threshold.
- Compaction preserves system prompt + recent messages.
- Second compaction is a no-op.

---

## S14.5 [G14] Session persistence round-trips messages in JSONL format
**Status:** done
**Test File:** `backend/src/runtime/session.rs` (inline `#[cfg(test)]`)
**Depends on:** G14, D23

1. Create `Session` in temp directory
2. Append 3 messages
3. Call `load()`
4. Assert all 3 messages loaded correctly

- JSONL format preserves message data.
- Append is incremental.
- Load returns all messages in order.

---

## S14.6 [G14] PermissionPolicy enforces modes correctly
**Status:** done
**Test File:** `backend/src/runtime/permission.rs` (inline `#[cfg(test)]`)
**Depends on:** G14, D23

1. Create `PermissionPolicy { mode: Allow }` → assert all tools allowed
2. Create `PermissionPolicy { mode: ReadOnly }` → assert write tools denied
3. Create `PermissionPolicy { mode: Ask }` → assert `AskUser` returned for all

- Allow mode permits everything.
- ReadOnly mode denies write operations.
- Ask mode defers to user.

---

## S15.1 [G15] CLI settings serialize and deserialize correctly
**Status:** done
**Test File:** `backend/src/cli/settings.rs` (inline `#[cfg(test)]`)
**Depends on:** G15, D22

1. Create full `Settings` with env, provider, model, api_key, base_url, permission
2. Serialize to JSON and deserialize back
3. Assert round-trip preserves all fields
4. Deserialize `{}` (minimal) → assert defaults

- Full settings round-trip without data loss.
- Empty JSON produces valid defaults.
- env HashMap is correctly populated.

---

## S15.2 [G15] Settings env injection respects existing env vars
**Status:** done
**Test File:** `backend/src/cli/settings.rs` (inline `#[cfg(test)]`)
**Depends on:** G15, D22

1. Set `TEST_VAR=existing`
2. Create `Settings { env: { "TEST_VAR": "new" } }`
3. Call `inject_env()`
4. Assert `TEST_VAR` still equals "existing" (not overwritten)

- Existing env vars are not overwritten by settings.
- Missing env vars are set from settings.

---

## S15.3 [G15] Full build passes with CLI module wired in
**Status:** done
**Test File:** CI (`cargo check && cargo test --lib`)
**Depends on:** G14, G15

1. Run `cargo check`
2. Assert 0 errors
3. Run `cargo test --lib`
4. Assert 131 passed, 1 pre-existing failure

- Build compiles without errors.
- All new and existing tests pass (except pre-existing `test_model_tier_serde`).

---

## S16.1 [G17] BringInTool validates target against handoff_to list
**Status:** proposed
**Test File:** `backend/src/forge/tools.rs` (inline `#[cfg(test)]`)
**Depends on:** G17, D24

```rust
let tool = BringInTool;
// Valid: advisor is in assistant's handoff_to
let valid = tool.execute(json!({"target": "advisor", "reason": "test"}));
// Invalid: architect is NOT in assistant's handoff_to
let invalid = tool.execute(json!({"target": "architect", "reason": "test"}));
```

1. Call `bring_in(target: "advisor")` with assistant as active profession
2. Assert result is Ok with "Handed off to Isaac (advisor)"
3. Call `bring_in(target: "architect")` with assistant as active profession
4. Assert result is Err with "Cannot hand off to 'architect'"
5. Call `bring_in(target: "advisor")` with advisor already active
6. Assert result is Err with "Already talking to advisor"

- Valid target succeeds.
- Target not in `handoff_to` is rejected.
- Target same as current profession is rejected.

---

## S16.2 [G17] bring_in switches session active_profession and persists
**Status:** proposed
**Test File:** `backend/src/forge/tools.rs` (inline `#[cfg(test)]`)
**Depends on:** G17, D24

```rust
let mut session = create_test_session();
session.active_profession = Some("assistant".into());
// Execute bring_in
let result = execute_bring_in(&mut session, "advisor", "NEW_GOAL", "User wants OAuth");
```

1. Assert `session.active_profession` changed from "assistant" to "advisor"
2. Assert a new system message was appended containing "Handoff Note"
3. Assert handoff note contains "Nicole" as from_agent
4. Assert handoff note contains "NEW_GOAL" as classification
5. Reload session from disk, assert `active_profession` persisted

- active_profession switches on successful bring_in.
- Handoff note injected into message history.
- Session state persists to disk.

---

## S16.3 [G17] HandoffNote renders correctly for downstream agent
**Status:** proposed
**Test File:** `backend/src/forge/mod.rs` (inline `#[cfg(test)]`)
**Depends on:** G17, D24

```rust
let note = HandoffNote {
    from_agent: "Nicole".into(),
    from_profession: "assistant".into(),
    to_profession: "advisor".into(),
    classification: "NEW_GOAL".into(),
    summary: "User wants to add OAuth2 login with JWT tokens.".into(),
    warnings: vec!["No existing auth code found".into()],
};
let rendered = note.render();
```

1. Assert rendered contains "Nicole (assistant) handed off to you"
2. Assert rendered contains "Classification: NEW_GOAL"
3. Assert rendered contains "OAuth2 login with JWT tokens"
4. Assert rendered contains "Warnings: No existing auth code found"
5. Assert rendered contains "do not ask the user to repeat"

- All fields render in the handoff note.
- Warnings render when present.
- Instruction to continue without repeating is included.

---

## S16.4 [G17] forge_stream uses new agent after bring_in
**Status:** proposed
**Test File:** `backend/src/forge/mod.rs` (integration test)
**Depends on:** G17, D24, P16.5

```rust
// Create session, send message, let Nicole classify and call bring_in
let mut session = create_test_session();
session.active_profession = Some("advisor".into());
// Forge_stream should use advisor system prompt for next turn
```

1. Create session with `active_profession: "assistant"`
2. Send message "I want to add OAuth2"
3. After Nicole's response (including bring_in call), assert `active_profession` is "advisor"
4. Send another message
5. Assert system prompt used for response contains "You are Isaac"
6. Assert system prompt contains "Soul of the Advisor"

- After bring_in, next turn uses target agent's system prompt.
- Target agent sees handoff note in context.
- Target agent sees full conversation history.

---

## S16.5 [G17] Frontend renders handoff card and updates profession badge
**Status:** proposed
**Test File:** `frontend/src/views/ChatsView.vue` (component test)
**Depends on:** G17, D24, P16.9

```typescript
// Simulate receiving agent_handoff SSE event
const event = { type: 'agent_handoff', from_agent: 'Nicole', to_agent: 'Isaac', ... }
```

1. Receive `agent_handoff` event during streaming
2. Assert HandoffCard component renders with "Nicole → Isaac"
3. Assert classification badge shows "NEW_GOAL"
4. Assert active profession badge in input area updates to "Isaac"
5. Assert bring_in tool_call does NOT render as generic tool output

- Handoff card renders for bring_in tool calls.
- Profession badge updates on handoff.
- No duplicate rendering (card replaces tool output).
