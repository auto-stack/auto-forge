# Goals

> AutoForge Agents module enables spec-driven serial cooperation of profession-bearing AI agents, maximizing quality and token efficiency through relay orchestration.


## Goals

| ID | Goal | Priority | Status | Stakeholder |
|---|---|---|---|---|
| G1 | Agent Identity System supports Soul, Profession, and Model configuration per agent | P0 | Done | Tooling Team |
| G2 | Relay Orchestrator executes serial agent flows with baton passing and gate enforcement | P0 | Implemented | Tooling Team |
| G3 | Handoff Protocol compresses context between agents, achieving ~5× token savings vs parallel multi-agent | P0 | Implemented | Tooling Team |
| G4 | Checkpoint & Durability system enables long-running flows to survive crashes and resume without context loss | P0 | Implemented | Tooling Team |
| G5 | Token Budgeting tracks and enforces per-step spend with cost analytics visible in Agents view | P1 | Implemented | Tooling Team |
| G6 | Boss-first execution: GSD mode runs autonomously with optional Check mode for gate review | P0 | Done | Tooling Team |
| G7 | Spec pyramid supports Goals → Architecture → Designs → Plans → Tests → Reviews → Reports with no APIs category | P0 | Done | Tooling Team |
| G8 | Chat-First Gate Approval with Smart Secretary | P0 | Implemented | UI Team |
| G9 | Specs Workspace with Bidirectional Traceability | P0 | Implemented | UI Team |
| G10 | Live Relay Pipeline Visualization | P0 | Implemented | UI Team |
| G11 | Responsive and Accessible Frontend | P1 | Implemented | UI Team |
| G12 | Project Concept enables VS Code–style folder opening with persistent workspace | P0 | Done | Core Team |
| G13 | API Source Configuration supports multi-provider LLM management with auto-detection and model tiers | P0 | Proposed | Core Team |
| G14 | Unified Provider Layer consolidates AI communication from auto-code-rs into provider/ module with SSE streaming | P0 | Done | Core Team |
| G15 | CLI Mode provides interactive REPL chat as forge chat subcommand | P1 | Removed | Core Team |
| G16 | @Mention Agent Targeting directs chat messages to named agents with sticky profession routing | P0 | Implemented | UI Team |
| G17 | Bring-In Tool enables agents to hand off mid-conversation with structured context transfer | P0 | Proposed | Core Team |
| G18 | Wiki module provides project-scoped knowledge base with LLM-compressed reference docs and semantic RAG search | P0 | Proposed | Docs Team |
| G19 | AutoDown editor enables structured markdown editing with Auto language syntax highlighting for Wiki content | P1 | Proposed | Docs Team |

---

## G1 Agent Identity System supports Soul, Profession, and Model configuration per agent
**Status:** implemented
**Depends on:** G7

- [x] Agents can be defined with a Soul (markdown character definition)
- [x] Agents can be assigned a Profession that determines spec ownership, tools, and handoff rules
- [x] Agents can be configured with a Model (provider, model name, temperature, max_tokens)
- [x] Built-in professions exist: Assistant, Advisor, Architect, Planner, Coder, Tester, Reviewer, Documenter
- [x] Custom professions can be added via `.autoforge/professions/` YAML files

The Agent Identity separates *who the agent is* (Soul), *what it can do* (Profession), and *what cognitive substrate it uses* (Model). This enables fine-grained control over agent behavior and cost optimization through model tiering.

---

## G2 Relay Orchestrator executes serial agent flows with baton passing and gate enforcement
**Status:** implemented
**Depends on:** G1

- [x] Flow specs define ordered steps with professions, gates, loops, and branches
- [x] Only one agent holds the baton at any time
- [x] Orchestrator enforces tool availability per profession
- [x] Orchestrator routes handoffs deterministically without LLM coordination cost
- [x] Loop bounds and error routing prevent infinite cycles

The Orchestrator is pure deterministic code (Rust), not an LLM. It reads Flow specs, manages the Pipeline state machine, and routes batons. This eliminates token spend on coordination and makes routing predictable.

---

## G3 Handoff Protocol compresses context between agents, achieving ~5× token savings vs parallel multi-agent
**Status:** implemented
**Depends on:** G1, G2

- [x] Handoff documents exclude raw chat history
- [x] Handoff documents include: summary, decisions, open questions, spec updates, work product, context pointers
- [x] Context scoping filters specs to only those relevant to the receiving profession
- [x] Each agent receives ≤9k tokens at spawn (Soul + relevant Specs + Handoff)
- [x] Agents pull additional context on demand via tools, not via push

The Handoff Protocol is the core token-efficiency mechanism. By discarding chat history and summarizing work product, agents start each turn with minimal context. Parallel multi-agent systems suffer O(N×K) token overhead; relay reduces this to O(sum of scoped contexts).

---

## G4 Checkpoint & Durability system enables long-running flows to survive crashes and resume without context loss
**Status:** implemented
**Depends on:** G2, G3

- [x] Every handoff produces a checkpoint with: git state, ledger state, handoff document, file manifest, token usage
- [x] Runs can resume from any checkpoint after process restart
- [x] Resume rehydrates the next agent from checkpoint (no chat history restored)
- [x] Rollback restores files, ledger, and state to a previous checkpoint
- [x] Fork creates a new run from an existing checkpoint with different parameters

Since agents "work longer rather than quicker," flows may run for hours or days. Checkpointing makes this safe. On resume, the next agent starts fresh from the Handoff Document — token cost is identical to continuous execution.

---

## G5 Token Budgeting tracks and enforces per-step spend with cost analytics visible in Agents view
**Status:** implemented
**Depends on:** G2

- [x] Per-step token budgets with configurable limit, warning threshold, and strategy
- [x] Budget strategies: HardStop, EscalateModel, SummarizeContext, SkipOptional
- [x] Agents view shows real-time token spend and "savings vs parallel" estimates
- [x] Cumulative run budget prevents runaway costs
- [x] Cost analytics persist across checkpoints

Token budgeting makes cost a first-class constraint. The Agents view compares actual relay cost against estimated parallel-swarm cost to demonstrate savings.

---

## G6 Boss-first execution: GSD mode runs autonomously with optional Check mode for gate review
**Status:** implemented
**Depends on:** G2

- [x] RelayMode enum exists: `GSD` (default) and `Check`
- [x] GSD mode: only GoalGate requires human approval; Design, Plan, Execution gates are auto
- [x] Check mode: every gate pauses for human review
- [x] Reviewer acts as internal quality auditor in GSD mode (no human gate)
- [x] Documenter compiles and delivers final Report as boss-facing product
- [x] Boss is interrupted only on ambiguity, blockage, budget exceed, or explicit Check mode

The human is the boss, not a coworker. The default behavior is autonomous execution. The boss gives one order and receives a Report. Human gates are opt-in, not mandatory.

---

## G7 Spec pyramid supports Goals → Architecture → Designs → Plans → Tests → Reviews → Reports with no APIs category
**Status:** implemented
**Depends on:** G1

- [x] APIs category removed from spec system
- [x] Interface specs (REST endpoints, schemas) live in Designs under `Interface` section
- [x] Architect owns Designs (including interfaces), not a separate APIs category
- [x] Documenter generates public API docs as product, not spec
- [x] Reports are relay execution summaries (cost, confidence, deliverables)

Specs are internal decision records (how to build). Products are shipped artifacts (code + public docs). Interface contracts are design specs; public API docs are generated products.

## G8 Chat-First Gate Approval with Smart Secretary
**Status:** implemented
**Depends on:** G6, G7

The chat is the boss's office. All gates, regardless of origin, are delivered here. The secretary only enters when a signature is needed or the job is done.

---

## G8.1 Inline GateCard renders diff view with Approve/Reject/Edit
**Status:** implemented
**Depends on:** G8

- [x] GateCard appears at the bottom of chat for current-session gates
- [x] Diff view shows what changed vs previous version with inline highlighting
- [x] Approve button advances pipeline; Reject button prompts for reason and routes back to Advisor
- [x] Edit escape hatch opens Specs drawer without losing gate state


---

## G8.2 SecretaryMessage delivers cross-session gates without blocking input
**Status:** implemented
**Depends on:** G8

- [x] Ephemeral messenger renders above input bar for gates from other sessions/runs
- [x] Input bar remains usable; secretary waits without interrupting
- [x] Message shows profession, session ID, and summary of proposed items
- [x] Resolved messages collapse to compact audit line after 3 seconds


---

## G8.3 Gate queue enforces one-at-a-time delivery with snooze and dismiss
**Status:** implemented
**Depends on:** G8

- [x] Only one secretary message is active at a time; additional gates enqueue
- [x] Queue pill shows "+N more" with expandable mini-list
- [x] Snooze moves gate to back of queue with configurable timer
- [x] Dismiss removes gate from active view but does not resolve it


---

## G8.4 Ambient progress indicators keep chat history free of status spam
**Status:** implemented
**Depends on:** G8

- [x] Session list shows pulsing dot + phase label for active relays
- [x] Chat history never contains interim "Architect done" messages
- [x] Errors are the only exception: single secretary message on unrecoverable failure
- [x] ReportCard renders terminal completion with metrics, cost, and deliverables


---

## G9 Specs Workspace with Bidirectional Traceability
**Status:** implemented
**Depends on:** G7, G8

Specs are not flat documents — they form a live decision pyramid. Every Plan traces back to a Goal, every Test verifies a Design. The frontend makes this traceability the primary navigation model.

---

## G9.1 Seven spec categories render with structured per-category editors
**Status:** implemented
**Depends on:** G9

- [x] Goals table with inline row editing
- [x] Architecture cards with Mermaid diagram rendering
- [x] Designs cards with interface signature preview
- [x] Plans timeline with Gantt-like phase table
- [x] Tests cards with pass/fail indicators
- [x] Reviews cards with criterion counts
- [x] Reports dashboard with metric bars


---

## G9.2 RelationsPanel shows upstream parents and downstream children
**Status:** implemented
**Depends on:** G9

- [x] Every expanded item shows Parents (depends_on) and Children (related) lists
- [x] Each relation entry shows ID, title, section type, and status badge
- [x] Clicking a relation jumps to target section, expands item, and flashes highlight
- [x] Backend auto-rebuilds related field on every document save


---

## G9.3 AutoLinkContent parses typed IDs into clickable hyperlinks
**Status:** implemented
**Depends on:** G9

- [x] Regex scans markdown content for typed ID references (`G1`, `A1`, `P1.3`)
- [x] Matching IDs render as clickable SpecLink components
- [x] Clicking a link triggers jumpToItem with section switch + scroll + flash
- [x] Non-ID text renders as normal markdown


---

## G9.4 Drift Check button compares code against specs on demand
**Status:** implemented
**Depends on:** G9

- [x] Drift Check button visible in Specs header
- [x] Runs comparison between Goals and implemented code
- [x] Flags mismatches with drift status on affected goals
- [x] Results render in a summary panel with severity indicators


---

## G10 Live Relay Pipeline Visualization
**Status:** implemented
**Depends on:** G6, G8

The Relay view is the factory floor CCTV. The boss glances at the session sidebar to know work is happening; engineers open Relay to diagnose handoffs, token burn, and tool call history.

---

## G10.1 Node-graph renders connected dots with animated handoff lines
**Status:** implemented
**Depends on:** G10

- [x] 9 pipeline steps render as horizontally connected dots on desktop
- [x] Dots are colored by status: pending=gray, working=amber pulse, done=green, error=red
- [x] Connectors animate with traveling dot during handoff
- [x] Mobile collapses to vertical timeline with CSS border-left connectors


---

## G10.2 Expanded node cards show real-time token burn and current action
**Status:** implemented
**Depends on:** G10

- [x] Clicking a node expands a card below with Overview/Actions/Output tabs
- [x] Overview tab shows tokens consumed, elapsed time, status, profession icon
- [x] Actions tab shows chronological tool call history
- [x] Output tab lists specs touched with click-to-jump links


---

## G10.3 Tester→Coder retry loop visualizes as backward edge with counter
**Status:** implemented
**Depends on:** G10

- [x] Backward arrow renders between Tester(verify) and Coder nodes
- [x] Arrow pulses amber when retry is active
- [x] Coder node shows "Retry N/3" badge during loop
- [x] After max iterations, error routes to Reviewer instead


---

## G10.4 Live Log and Cost Breakdown panels stream relay diagnostics
**Status:** implemented
**Depends on:** G10

- [x] Live Log streams tool calls in real time with timestamp, profession, action
- [x] Specs Touched panel shows mutated sections with click-to-jump
- [x] Cost Breakdown shows per-profession token bars and cumulative spend
- [x] All panels update via SSE without page refresh


---

## G11 Responsive and Accessible Frontend
**Status:** implemented
**Depends on:** G8, G9, G10

The frontend must work for the boss on a laptop, the engineer on a tablet, and the contributor with a keyboard or screen reader.

---

## G11.1 Layout adapts to desktop, tablet, and mobile breakpoints
**Status:** implemented
**Depends on:** G11

- [x] Desktop (≥1280px): full 3-zone layout with sidebar + main + context panel
- [x] Tablet (1024–1279px): Zone C collapses to toggle drawer
- [x] Mobile (<1024px): single column with tab nav and bottom sheets
- [x] No horizontal scroll on any breakpoint


---

## G11.2 Keyboard shortcuts navigate between views without mouse
**Status:** implemented
**Depends on:** G11

- [x] Ctrl+1 switches to Chat view
- [x] Ctrl+2 switches to Specs view
- [x] Ctrl+3 switches to Relay view
- [x] Ctrl+K opens jump-to-ID dialog
- [x] Shortcuts work from any focused element


---

## G11.3 Focus management guides users through gate approval flows
**Status:** implemented
**Depends on:** G11

- [x] GateCard auto-focuses Approve button when it appears
- [x] Secretary message receives focus on arrival; Escape dismisses
- [x] Tab order is logical within diff views and expanded cards
- [x] Focus traps do not prevent reaching browser chrome


---

## G11.4 Screen reader annotations describe node graph and relations
**Status:** implemented
**Depends on:** G11

- [x] Pipeline nodes have aria-label with profession + status + elapsed time
- [x] RelationsPanel announces "X parents, Y children" on expand
- [x] Live Log announces new entries with polite live region
- [x] All interactive elements have accessible names

## G12 Project Concept enables VS Code–style folder opening with persistent workspace
**Status:** done
**Depends on:** G7, G9

- [x] Backend SpecsStore starts empty (no auto-load from current_dir)
- [x] Project opens via API: POST /api/forge/project/open with directory path
- [x] Specs auto-discovered from {project}/specs/ with fallback to legacy docs/specs/
- [x] Project config persisted (last project, recent list) in user data dir
- [x] Backend auto-restores last project on restart
- [x] Frontend shows WelcomeView when no project is open
- [x] Path input with directory browsing autocomplete
- [x] Recent projects list for quick re-opening
- [x] Project indicator (name + close button) in navigation rail
- [x] SpecsView and ChatsView use dynamic project from useProject composable
- [x] docs/specs/ promoted to top-level specs/ directory

AutoForge operates on a user-chosen project directory, just like VS Code. On first launch the workspace is empty; the user opens a folder and specs are loaded from its specs/ subdirectory. The last project is remembered across restarts.

---

## G13 API Source Configuration supports multi-provider LLM management with auto-detection and model tiers
**Status:** proposed
**Depends on:** G1, G6

- [ ] ApiSource data model supports Anthropic, OpenAI, and Local providers with per-source models
- [ ] ModelTier enum (Light / Mid / Heavy) maps to concrete models per provider
- [ ] First-startup auto-detection finds existing Claude keys, OpenAI keys, and Ollama instances
- [ ] API sources persisted to `dirs::data_local_dir()/autoforge/api_sources.json`
- [ ] CRUD API endpoints for creating, reading, updating, and deleting API sources
- [ ] Multi-provider AI dispatch routes chat requests to the correct provider (Anthropic SSE, OpenAI streaming, Local/Ollama)
- [ ] API key resolution priority: env var → ~/.claude/settings.json → stored (base64)
- [ ] Test Connection endpoint verifies API source connectivity and credentials
- [ ] APIs top-level view with source list, detail editor, and model management table
- [ ] First-startup UI guides user when no API source is configured

Currently AutoForge only supports Anthropic Claude with a hardcoded API key path. G13 introduces a configurable multi-provider system so agents can use different LLM backends. The APIs view sits as a top-level navigation tab alongside Chat, Specs, and Relay, because Tools and Skills views will follow the same pattern in the future.

---

## G14 Unified Provider Layer consolidates AI communication from auto-code-rs into provider/ module with SSE streaming
**Status:** done
**Depends on:** G1

- [x] Unified message types (ChatMessage, ContentBlock, StreamEvent, ToolChatEvent) in provider/types.rs
- [x] Robust incremental SSE parser (SseParser) in provider/sse.rs handling both \r\n\r\n and \n\n delimiters
- [x] Single ClaudeProvider with chat_stream() and chat_turn() methods replacing both ai.rs and forge/ai.rs
- [x] ToolError enum (ExecutionFailed, InvalidInput, PermissionDenied) for structured tool error handling
- [x] ContextManager with token estimation and automatic compaction
- [x] Session persistence in JSONL format at ~/.autoforge/sessions/
- [x] PermissionPolicy with Allow/Ask/ReadOnly modes

The provider/ module is the unified AI communication layer, ported from auto-code-rs and merged with auto-forge's existing Claude integration. It replaces both the old ai.rs and forge/ai.rs modules, providing a single entry point for all LLM interactions with structured types and robust SSE parsing.

---

## G15 CLI Mode provides interactive REPL chat as forge chat subcommand
**Status:** draft
**Depends on:** G14

CLI mode was ported from auto-code-rs but removed because AutoForge is a Web UI-driven system — a CLI REPL is not part of its core workflow. The provider, runtime, and tool modules remain; only the CLI entry point and REPL loop were removed.

---

## G16 @Mention Agent Targeting directs chat messages to named agents with sticky profession routing
**Status:** implemented
**Depends on:** G1, G12

- [x] `@DisplayName` syntax in chat input resolves to profession_id via AgentConfig lookup
- [x] Autocomplete dropdown filters agents by display name and profession_id after typing `@`
- [x] Session stores `active_profession` — subsequent messages without `@` keep targeting the same agent
- [x] Backend resolves profession to full agent (soul + profession system prompt) via RelayRegistry
- [x] Agent identifies by display name in system prompt and responses (e.g., "I'm Nicole")
- [x] @mentions render as inline styled chips in both input backdrop and message bubbles
- [x] Tool role messages hidden from chat display; bouncing dots animation replaces empty assistant bubble
- [x] Enter sends, Ctrl+Enter inserts newline
- [x] Tool names renamed from jade to specs (list_jade→list_specs, etc.)
- [x] All tools resolve file paths via project directory instead of backend CWD

## G16.1 Display name resolution and identity
**Status:** implemented
**Depends on:** G16

- [x] AgentConfig `name` field serves as display name (e.g., "Nicole", "Isaac")
- [x] `resolveMention()` maps both display names and profession_ids to canonical profession_id
- [x] `AgentInstance.display_name` propagated through `spawn_agent_from_config()`
- [x] System prompt leads with "You are {display_name}" instead of "AutoForge"

## G16.2 @Mention autocomplete with backdrop overlay
**Status:** implemented
**Depends on:** G16

- [x] MentionDropdown component with keyboard navigation (up/down/enter/escape)
- [x] `@` detection scans backwards from cursor position (not limited to start of input)
- [x] Backdrop overlay technique renders styled @mention chips behind transparent textarea
- [x] Selection replaces `@filter` with `@DisplayName ` at cursor position

---

## G17 Bring-In Tool enables agents to hand off mid-conversation with structured context transfer
**Status:** proposed
**Depends on:** G1, G16

- [ ] `bring_in` tool available to assistant (Nicole) during chat, accepting target profession and reason
- [ ] Tool switches `session.active_profession` and injects handoff note into chat history before next agent's turn
- [ ] Handoff note includes: classification, summary of understanding, warnings, and context pointers
- [ ] Next agent starts with its own system prompt + full conversation history + handoff note (no repeated questions)
- [ ] Frontend shows handoff event in chat (e.g., "Nicole brought in Isaac" with reason)
- [ ] Profession `allowed_tools` enforcement — only professions that have `bring_in` in their tool list can invoke it
- [ ] Handoff restricted to professions listed in `handoff_to` (Nicole can hand off to advisor/coder, not arbitrary targets)

## G17.1 bring_in tool definition and backend handler
**Status:** proposed
**Depends on:** G17

- [ ] `BringInTool` struct with parameters: `target` (profession_id), `reason` (string), `classification` (optional enum)
- [ ] Tool handler switches `session.active_profession`, persists handoff note as a system message
- [ ] Handoff note rendered from a lightweight HandoffDocument (summary + decisions + context pointers)
- [ ] Assistant profession `allowed_tools` updated to include `bring_in`
- [ ] Validation: target must be in caller's `handoff_to` list
- [ ] Session saved after handoff so state survives reconnects

## G17.2 Handoff note injection into conversation
**Status:** proposed
**Depends on:** G17.1

- [ ] Before the new agent's first turn, backend injects a system message with the handoff note
- [ ] Handoff note format: "Nicole handed off to you. Classification: NEW_GOAL. Summary: User wants OAuth2 login. Warnings: No existing auth code found."
- [ ] New agent sees full conversation history + handoff note (does not ask user to repeat)
- [ ] Handoff system messages excluded from frontend display (shown as handoff card instead)

## G17.3 Frontend handoff visualization
**Status:** proposed
**Depends on:** G17.1

- [ ] Handoff card in chat stream showing "Nicole → Isaac" with reason and classification badge
- [ ] Active profession badge updates when handoff occurs (phase_change or dedicated SSE event)
- [ ] Handoff tool_call and tool_result rendered as a compact card, not raw tool output

Perfect! Here's the exact content to append to `specs/goals.ad`:

---

## G18 Wiki module provides project-scoped knowledge base with LLM-compressed reference docs and semantic RAG search
**Status:** proposed
**Depends on:** G12

- [ ] Wiki storage in `specs/wiki/` or `.autoforge/wiki/` (project-scoped, one wiki per project)
- [ ] LLM-compressed markdown entries (5-10 page summaries from 3000-page manuals)
- [ ] Semantic vector indexing (Chroma or FAISS) for RAG retrieval
- [ ] Agent wiki tool: `query_wiki` (semantic search, returns relevant passages)
- [ ] Agent wiki tool: `create_wiki_page` and `update_wiki_page` (unrestricted editing with versioning)
- [ ] Wiki supports hierarchical organization (folders) + tags
- [ ] Version control: full history with revert capability
- [ ] Left column `Wiki` tab alongside `Chats` and `Specs`
- [ ] Three-column layout: nav (left), content (middle), metadata/right (right)
- [ ] Content types: chip manuals, register tables, how-to guides, API references
- [ ] Token savings: ~87.5% (500 token RAG retrieval vs 4000 token full manual)
- [ ] Offline, local operation (no external network dependency)

Wiki is AutoForge's **knowledge index & compression layer**. It stores objective, reusable reference material (chip manuals, technical specs, best practices) — separate from Specs which store project-specific engineering constraints. Agents use semantic RAG to retrieve only relevant passages, dramatically reducing token cost compared to loading full documentation.

---

## G19 AutoDown editor enables structured markdown editing with Auto language syntax highlighting
**Status:** proposed
**Depends on:** G18

- [ ] Markdown-based editor (baseline)
- [ ] Auto language syntax extensions: type definitions, function signatures, struct definitions, Flow syntax (`$Flow`, `$Step`)
- [ ] Live preview with syntax highlighting
- [ ] Used for Wiki content editing
- [ ] Future: may extend to other spec sections

AutoDown is Markdown extended with Auto language syntax, providing structured editing capabilities for Wiki content. Initially supports basic Markdown with Auto language highlighting; future versions may add more sophisticated structured editing features.

---

**Note for the bug fix**: The `write_specs` tool should:
1. Update the in-memory specs state (current behavior)
2. **Also** persist to the actual `.ad` file at `specs/{section_id}.ad`
3. Handle both creating new files and appending to existing ones
4. Preserve file formatting and existing content structure

Let me know once you've added these and fixed the bug, and I can continue with the next phase of planning!
