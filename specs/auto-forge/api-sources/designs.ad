# Designs

---

## ApiSources-D1 模型优先的智能体配置设计
**Status:** draft

**状态：** 已批准
**模块：** `backend/src/relay/config.rs`、`frontend/src/composables/useAgentConfigs.ts`、`frontend/src/views/AgentsConfigView.vue`
**标签：** stack:both, module:api-sources
**依赖：** ApiSources-A3, ApiSources-G3

### 概述

将 `AgentConfig` 中的 `model_tier` 替换为 `model_id`，以实现直接模型选择。这是一次数据模型变更、解析逻辑简化以及前端 UI 更新。

### 1. 后端数据模型变更

**文件：`backend/src/relay/config.rs`**

#### AgentConfig（当前第 379–397 行）

将 `model_tier: ModelTier` 替换为 `model_id: String`，保留 `model_tier` 作为迁移用的弃用字段：

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AgentConfig {
    pub id: String,
    pub name: String,
    pub profession_id: String,
    pub soul_id: String,
    pub api_source_id: String,
    /// Direct model reference (e.g., "claude-sonnet-4-20250514"). Replaces model_tier.
    #[serde(default)]
    pub model_id: String,
    /// Deprecated: kept for migration from legacy configs. No longer used in resolution.
    #[serde(default)]
    pub model_tier: ModelTier,
    #[serde(default)]
    pub is_default: bool,
    #[serde(default = "default_temperature")]
    pub temperature: f32,
    #[serde(default = "default_max_tokens")]
    pub max_tokens: u32,
    #[serde(default)]
    pub reasoning_budget: Option<u32>,
    #[serde(default)]
    pub thinking_enabled: bool,
    #[serde(default)]
    pub thinking_budget: Option<u32>,
    #[serde(default)]
    pub avatar_url: Option<String>,
    #[serde(default)]
    pub equipped_skills: Vec<String>,
}
```

**关键**：两个字段都使用 `#[serde(default)]`，因此仅包含 `model_tier` 的现有 JSON 文件可以无错误地反序列化（model_id 默认为 ""），而仅包含 `model_id` 的新文件也能正常工作（model_tier 默认为 Min）。

#### ModelDefinition（第 32–36 行）—— 未变更

```rust
pub struct ModelDefinition {
    pub id: String,       // e.g., "claude-sonnet-4-20250514"
    pub name: String,     // e.g., "Claude Sonnet 4"
    pub tier: ModelTier,  // Display tag only: Min/Lite/Mid/Pro/Max
}
```

#### resolve_model()（当前第 548–586 行）—— 简化

将整个层级降级遍历替换为直接的 model_id 查找：

```rust
pub fn resolve_model(
    config: &AgentConfig,
    api_sources: &[ApiSource],
) -> Option<crate::relay::agent::ModelConfig> {
    // 1. Find the ApiSource
    let source = api_sources.iter()
        .find(|s| s.id == config.api_source_id)
        .or_else(|| api_sources.first())?;

    // 2. Find the specific model by ID
    let model_def = source.models.iter()
        .find(|m| m.id == config.model_id)
        .or_else(|| {
            log::warn!(
                "model_id '{}' not found in ApiSource '{}', falling back to first model",
                config.model_id, source.id
            );
            source.models.first()
        })?;

    Some(crate::relay::agent::ModelConfig {
        provider: source.provider.clone(),
        model: model_def.id.clone(),
        temperature: config.temperature,
        max_tokens: config.max_tokens,
        reasoning_budget: config.reasoning_budget,
        fallback_chain: Vec::new(),
    })
}
```

#### generate_default_agents_with_source()（当前第 449–490 行）—— 已更新

根据首选层级选择具体的 model_id：

```rust
pub fn generate_default_agents_with_source(api_source_id: &str) -> Vec<AgentConfig> {
    // Note: api_sources are loaded separately; this function is called
    // from load_or_generate_agent_configs() which has access to them.
    // We generate with empty model_id; load_or_generate_agent_configs
    // will call migrate_agent_config() to fill it.
    let defaults: [(&str, &str, &str, ModelTier); 9] = [
        ("assistant", "Nicole", "assistant", ModelTier::Lite),
        ("advisor", "Isaac", "advisor", ModelTier::Mid),
        ("architect", "Vera", "architect", ModelTier::Pro),
        ("planner", "Felix", "planner", ModelTier::Mid),
        ("tester", "Quinn", "tester", ModelTier::Lite),
        ("coder", "Ash", "coder", ModelTier::Mid),
        ("reviewer", "Marcus", "reviewer", ModelTier::Pro),
        ("documenter", "Luna", "documenter", ModelTier::Lite),
        ("gofer", "Gus", "gofer", ModelTier::Lite),
    ];

    defaults
        .map(|(profession, name, soul, preferred_tier)| AgentConfig {
            id: format!("default-{}", profession),
            name: name.to_string(),
            profession_id: profession.to_string(),
            soul_id: soul.to_string(),
            api_source_id: api_source_id.to_string(),
            model_id: String::new(), // filled by migration
            model_tier: preferred_tier,
            is_default: true,
            temperature: 0.3,
            max_tokens: if preferred_tier == ModelTier::Lite { 4096 } else { 8192 },
            reasoning_budget: if preferred_tier == ModelTier::Pro { Some(4096) } else { None },
            thinking_enabled: matches!(profession, "advisor" | "architect" | "planner" | "tester" | "coder" | "reviewer"),
            thinking_budget: match profession {
                "architect" | "coder" => Some(2048),
                "advisor" | "planner" | "tester" | "reviewer" => Some(1024),
                _ => None,
            },
            avatar_url: None,
            equipped_skills: Vec::new(),
        })
        .to_vec()
}
```

新增的 `assign_model_ids()` 函数在生成后填充 model_id：

```rust
/// For each config with empty model_id, find the best matching model
/// from its ApiSource by preferred tier, then fallback to first model.
pub fn assign_model_ids(configs: &mut [AgentConfig], api_sources: &[ApiSource]) {
    for config in configs.iter_mut() {
        if config.model_id.is_empty() {
            let source = api_sources.iter()
                .find(|s| s.id == config.api_source_id)
                .or_else(|| api_sources.first());
            config.model_id = source
                .and_then(|s| {
                    s.models.iter().find(|m| m.tier == config.model_tier)
                        .or_else(|| s.models.first())
                })
                .map(|m| m.id.clone())
                .unwrap_or_default();
        }
    }
}
```

#### 迁移 —— 在 load_or_generate_agent_configs() 中（当前第 498–527 行）

在加载现有配置后插入 `assign_model_ids()` 调用：

```rust
pub fn load_or_generate_agent_configs(api_sources: &[ApiSource]) -> Vec<AgentConfig> {
    let existing = load_agent_configs();
    let defaults = generate_default_agents_with_source(
        api_sources.first().map(|s| s.id.as_str()).unwrap_or(""),
    );

    let mut merged = if existing.is_empty() {
        defaults
    } else {
        // Fix empty api_source_id
        let first_source_id = api_sources.first().map(|s| s.id.as_str()).unwrap_or("");
        let mut merged: Vec<AgentConfig> = existing.into_iter().map(|mut c| {
            if c.api_source_id.is_empty() && !first_source_id.is_empty() {
                c.api_source_id = first_source_id.to_string();
            }
            c
        }).collect();

        // Add missing defaults
        for default in &defaults {
            if !merged.iter().any(|c| c.id == default.id) {
                merged.push(default.clone());
            }
        }
        merged
    };

    // MIGRATION: Fill model_id for configs that only have model_tier
    assign_model_ids(&mut merged, api_sources);

    let _ = save_agent_configs(&merged);
    merged
}
```

### 2. 前端变更

**文件：`frontend/src/composables/useAgentConfigs.ts`**

```typescript
export interface AgentConfigDto {
  id: string
  name: string
  profession_id: string
  soul_id: string
  api_source_id: string
  model_id: string             // ← NEW: direct model reference (was model_tier)
  model_tier: 'min' | 'lite' | 'mid' | 'pro' | 'max'  // ← kept for display
  is_default: boolean
  temperature: number
  max_tokens: number
  reasoning_budget: number | null
  thinking_enabled: boolean
  thinking_budget: number | null
  avatar_url?: string
  equipped_skills?: string[]
}
```

**文件：`frontend/src/composables/useApiSources.ts`** —— 添加辅助函数

```typescript
/// Get models for a specific API source
function getModelsForSource(sourceId: string): ModelDefinition[] {
  const source = _sources.value.find(s => s.id === sourceId)
  return source?.models ?? []
}
```

**文件：`frontend/src/views/AgentsConfigView.vue`**

将层级下拉框替换为模型选择器：

```
┌─────────────────────────────────────────────┐
│ Agent: Isaac (advisor)                       │
├─────────────────────────────────────────────┤
│ API Source: [Anthropic Claude ▾]            │
│                                              │
│ Model: [Claude Sonnet 4 (Mid) ▾]            │
│        ┌──────────────────────────────┐      │
│        │ ● Claude Haiku 3.5    (Min)  │      │
│        │ ○ Claude Sonnet 3.5  (Lite)  │      │
│        │ ● Claude Sonnet 4    (Mid) ✓ │      │
│        │ ○ Claude Opus 4      (Pro)   │      │
│        └──────────────────────────────┘      │
│                                              │
│ Temperature: [0.3]  Max Tokens: [8192]      │
└─────────────────────────────────────────────┘
```

1. 当 `api_source_id` 变更时 → 刷新模型列表，自动选择第一个模型
2. 模型列表显示：显示名称 + 层级徽章（颜色编码）
3. 折叠选择器中显示已选模型的层级徽章
4. 层级徽章颜色：Min=灰色，Lite=绿色，Mid=蓝色，Pro=紫色，Max=琥珀色

#### 层级徽章 CSS 类

```css
.tier-badge.min { background: #9ca3af; color: white; }  /* gray */
.tier-badge.lite { background: #22c55e; color: white; }  /* green */
.tier-badge.mid { background: #3b82f6; color: white; }   /* blue */
.tier-badge.pro { background: #a855f7; color: white; }   /* purple */
.tier-badge.max { background: #f59e0b; color: white; }   /* amber */
```

### 3. 状态机

```mermaid
stateDiagram-v2
    [*] --> Idle: AgentConfig loaded
    Idle --> SourceChanged: User changes api_source_id
    SourceChanged --> ModelListLoaded: Fetch models for new source
    ModelListLoaded --> ModelSelected: Auto-select first model OR user picks
    ModelSelected --> Saved: User saves config
    ModelSelected --> SourceChanged: User changes source again
    Saved --> [*]

    Idle --> ModelSelected: User changes model_id (same source)
    ModelListLoaded --> SourceChanged: User changes source before selecting
```

### 4. 错误处理

| 错误场景 | 处理方式 |
|----------|----------|
| ApiSource 中未找到 `model_id` | 回退到来源中的第一个模型，通过 `log::warn!` 记录警告 |
| 未找到 `api_source_id` | 回退到第一个 ApiSource + 第一个模型 |
| ApiSource 没有模型 | `resolve_model()` 返回 `None`，智能体启动优雅失败 |
| 遗留配置有 `model_tier` 但无 `model_id` | 加载时通过 `assign_model_ids()` 自动迁移：层级 → 第一个匹配的模型 |
| 智能体引用 ApiSource 时该来源被删除 | `resolve_model()` 回退到第一个 ApiSource；UI 显示"来源未找到" |

### 5. 修改的文件

| 文件 | 变更 |
|------|------|
| `backend/src/relay/config.rs` | 向 AgentConfig 添加 `model_id: String`；简化 `resolve_model()` 为直接查找；添加 `assign_model_ids()` 用于迁移；更新 `generate_default_agents_with_source()` |
| `backend/src/relay/mod.rs` | 无逻辑变更 —— `RelayRegistry` 已对所有模型解析使用 `resolve_model()` |
| `frontend/src/composables/useAgentConfigs.ts` | 向 `AgentConfigDto` 添加 `model_id`；保留 `model_tier` 用于显示 |
| `frontend/src/composables/useApiSources.ts` | 添加 `getModelsForSource(sourceId)` 辅助函数 |
| `frontend/src/views/AgentsConfigView.vue` | 将层级下拉框替换为模型选择器，列出所选 ApiSource 的模型并显示层级徽章 |
| `frontend/src/locales/en.json` | 添加 `agents.model` 键 |
| `frontend/src/locales/zh.json` | 添加 `agents.model` 键 |

### 6. 数据生命周期

```
[默认生成]
  api_sources 加载 → generate_default_agents_with_source() → model_id 留空
  → assign_model_ids() 根据首选层级匹配填充 model_id
  → 保存到 agent_configs.json

[用户编辑]
  用户打开 AgentConfig → UI 显示所选 ApiSource 的模型列表
  → 用户选择模型 → 保存 model_id；model_tier 根据所选模型的层级更新

[智能体启动]
  Orchestrator → resolve_model(config, api_sources)
  → 通过 model_id 查找 ModelDefinition（直接查找，无层级遍历）
  → 返回 ModelConfig(provider, model_id, params)
  → 使用解析后的模型启动 AgentInstance

[ApiSource 模型变更]
  如果引用的 model_id 从 ApiSource 中移除
  → resolve_model 回退到第一个模型 + 记录警告
  → UI 在 AgentConfig 卡片上显示"模型未找到"指示器

[遗留配置迁移]
  加载 agent_configs.json → model_id 为空，model_tier 存在
  → assign_model_ids() 将层级转换为 ApiSource 中第一个匹配的模型
  → 保存回文件，model_id 已填充
```

### 7. 已验证的代码证据

| 内容 | 文件:行号 |
|------|-----------|
| 包含 `model_tier` 的 AgentConfig 结构体 | `backend/src/relay/config.rs:379-397` |
| 带层级降级遍历的 `resolve_model()` | `backend/src/relay/config.rs:548-586` |
| `generate_default_agents_with_source()` | `backend/src/relay/config.rs:449-490` |
| `load_or_generate_agent_configs()` | `backend/src/relay/config.rs:498-527` |
| 包含 `model_tier` 的 `AgentConfigDto` | `frontend/src/composables/useAgentConfigs.ts:8-21` |
| `ModelDefinition` 接口 | `frontend/src/composables/useApiSources.ts:6-10` |
| 卡片模板中的层级徽章 | `frontend/src/views/AgentsConfigView.vue:47` |
| `tierLabel()` 辅助函数的使用 | `frontend/src/views/AgentsConfigView.vue:47` |
| `RelayRegistry` 使用 `resolve_model()` | `backend/src/relay/mod.rs:48-53` |

## ApiSources-D2 API 文档视图
**Status:** draft

**状态：** 已实现
**模块：** `frontend/src/views/ApiSourcesView.vue` / `specs/apis.ad`
**标签：** stack:frontend
**依赖：** ApiSources-A2

- 将 `apis.ad` 渲染为结构化的端点文档
- 按后端模块分组端点，使用可折叠区域
- 为每个路由显示方法徽章、路径和处理程序名称

- `useSpecs.ts` 用于加载 `apis.ad` 章节
- `MarkdownContent.vue` 用于渲染 Markdown 表格

---

---

---

---

---

---

---

---

---

## ApiSources-T3 详细说明
**Status:** draft

**状态：** 草稿

应保留标记为"完成"的最佳副本，其需具备：
- 表格中包含全部 15 个测试用例（TC-T3.1 至 TC-T3.15）
- 将 G3 目标映射到测试的覆盖矩阵
- `### Rust Test Code` 章节中包含完整的 Rust 测试代码
- 后端和前端的实现验证清单

其余 9 个以上的副本（各种状态：草稿、完成、提议）必须删除。

### 修改的文件

不修改任何代码文件。这是一次**仅限规范的清理**。

| 操作 | 章节 | 项目 ID | 要删除的副本 |
|------|------|---------|-------------|
| 删除 | tests | ApiSources-T3 | 9+ 个副本（保留最佳的"完成"版本） |
| 删除 | designs | ApiSources-D1 | 3 个副本（保留带代码证据的已批准版本） |
| 删除 | designs | UiSystem-D4 | 4 个副本（保留最完整的草稿） |
| 删除 | architecture | ApiSources-A3 | 4 个副本（保留第一个完整版本） |

### 预防措施（未来）

`update_spec` 工具已能按 ID 正确执行 upsert 操作。重复来源于接力智能体在单次流水线运行中多次写入*相同 ID*。修复方案为：

1. 编码智能体应对已有项目调用 `update_spec(action: "upsert")`，而非 `write_specs`
2. 测试智能体应在创建测试项目前检查 `list_specs`
3. 如果存在相同 ID 的项目，使用 `update_spec(action: "patch")` 仅更新内容

### 触发条件

立即执行 —— 这是由"调试测试"任务触发的手动清理。未来通过智能体行为自动预防。

### 数据流

```
[Tests 章节已加载] → [分组 ApiSources-T3 项目] → [选择带有完整测试代码的"完成"版本] → [删除 9+ 个重复项] → [保存干净的章节]
```

### 设计要点

这是一次性修复，而非系统功能。真正的修复是智能体行为变更（使用 upsert，而非 append）。去重只是清理已存在的混乱。

| 方案 | 优点 | 缺点 |
|------|------|------|
| 手动规范清理（已选中） | 即时、精确、无代码风险 | 繁琐，无法防止再次发生 |
| 自动化去重脚本 | 可扩展、可重复 | 对于一次性清理来说过于复杂 |
| 仅智能体预防 | 防止未来发生 | 无法修复当前状态 |
