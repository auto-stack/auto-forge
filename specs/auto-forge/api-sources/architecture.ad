# Architecture

---

## ApiSources-A1 API 来源与多提供商架构
**Status:** draft

**状态：** 提议中
**标签：** stack:both
**依赖项：** ApiSources-G1

单一的硬编码提供商（仅 Claude）限制了智能体的灵活性和成本优化。不同角色的智能体有不同的认知需求 —— 路由助手需要便宜/快速的模型，而架构师需要强大的推理模型。多提供商支持允许从任何已配置的 API 来源中为每个智能体选择模型。

```mermaid
graph TB
    subgraph "配置层"
        CF[config.rs] -->|加载| AS[ApiSource 存储]
        CF -->|加载| AC[AgentConfig 存储]
    end
    subgraph "提供商分发"
        DP[dispatch_chat] --> AP[Anthropic 提供商]
        DP --> OP[OpenAI 提供商]
        DP --> LP[本地/Ollama 提供商]
    end
    subgraph "流水线"
        PE[PipelineEngine] -->|解析| RC[RelayRegistry]
        RC -->|派生自| AC
        AC -->|使用| AS
        AS -->|通过| DP
    end

    PE -->|步骤| AG[AgentInstance]
    AG -->|聊天通过| DP
```

### 触发条件
当智能体被派生时，`resolve_model()` 查找 AgentConfig 的 `model_id` 在引用的 ApiSource 中，然后 `dispatch_chat()` 路由到正确的提供商。

### 数据流
`[AgentConfig.model_id] → [resolve_model() → 在 ApiSource.models 中查找] → [dispatch_chat(provider, model)] → [SSE 流]`

### 设计亮点
多提供商分发允许为每个智能体选择模型。OpenAI 和本地共享相同的 `/v1/chat/completions` 格式；只有 Anthropic 不同。这将 SSE 解析路径从 3 个减少到 2 个。

| 方案 | 优点 | 缺点 |
|---|---|---|
| 带 ApiSource 配置的多提供商分发器 | 每个智能体可选模型，成本优化 | AI 分发层更复杂 |
| 仅带模型层级的单一提供商 | 实现更简单 | 锁定在一个提供商 |
| 运行时模型注册表（无配置 UI） | 无需 UI | 用户无法管理 API 密钥 |

- 正面：每个智能体可以为其任务使用最优的提供商 + 模型。
- 正面：首次启动自动检测找到现有密钥 —— 大多数用户零配置。
- 负面：必须维护三种不同的 SSE 解析路径。
- 缓解：OpenAI 和本地共享相同格式；只有 Anthropic 不同。

## ApiSources-A2 API 文档视图架构
**Status:** draft

**状态：** 已批准
**标签：** stack:frontend

- 静态文档位于 `specs/apis.ad`；在前端以只读视图渲染
- 按后端模块分组：Forge、Relay、Config、Wiki、Raw
- 每个条目显示 HTTP 方法、路由路径、处理函数和一行描述
- 当 `apis.ad` 更新时，可通过规范工作空间编辑

## ApiSources-A3 ApiSources-A3 Model-First Agent Config Architecture
**Status:** approved
**Module:** api-sources
**Tags:** stack:both, module:api-sources
**Depends on:** ApiSources-G1, ApiSources-G3

## Overview

Replace tier-based model selection (`model_tier: ModelTier`) in `AgentConfig` with direct model references (`model_id: String`). This eliminates the fragile tier-degradation walk and ensures every agent resolves to a concrete, existing model.

## Problem

The current `resolve_model()` walks down tiers (Min → Lite → Mid → Pro → Max) looking for any model present in the ApiSource. If the ApiSource only has Mid and Pro models, an agent configured for Lite silently gets Mid — potentially more expensive than intended. The user has no visibility into this; agent quality degrades unpredictably.

## Solution

Each `AgentConfig` stores a concrete `model_id` (e.g., `"claude-sonnet-4-20250514"`). Resolution becomes a single lookup — no tier walk. If model_id doesn't exist in the ApiSource, fall back to the first model and log a warning. Tier becomes a display-only tag on `ModelDefinition`.

```mermaid
graph TB
    subgraph "Config Resolution"
        AC[AgentConfig.model_id] --> RM[resolve_model]
        AS[ApiSource.models] --> RM
        RM --> MC[ModelConfig]
    end
    subgraph "Migration Path"
        LEGACY[Legacy: model_tier only] --> AMI[assign_model_ids]
        AMI -->|tier → first matching model| AC
        NEW[New: model_id set] --> AC
    end
    subgraph "Fallback Chain"
        RM -->|Found model_id| MC
        RM -->|Missing model_id| LOG[log::warn + first model]
        RM -->|Missing source| LOG2[log::warn + first source]
    end
```

### Trigger Condition

`resolve_model()` is called every time an agent is spawned (relay step or chat turn). Migration runs once during `load_or_generate_agent_configs()`.

### Data Flow

```
[AgentConfig.model_id] + [ApiSource.models]
    → resolve_model() → lookup ModelDefinition by model_id
    → ModelConfig { provider, model, temperature, max_tokens, reasoning_budget }
```

### Design Highlight

Direct model reference is simpler, more predictable, and gives users precise control. Tier degradation walk was a hidden behavior that surprised users when their "Lite" agent was silently upgraded to "Mid".

### Key Architecture Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Model reference | `model_id: String` on AgentConfig | Direct, unambiguous, no silent degradation |
| Tier role | Display tag on ModelDefinition | Users see grouping; resolution ignores it |
| Migration strategy | Auto-populate on load | Zero user action; backward compatible |
| Fallback | First model in source + log warning | Always produces a valid config; visible in logs |

### Trade-offs

| Approach | Pros | Cons |
|----------|------|------|
| **Direct model_id reference (chosen)** | No silent degradation, simple resolution, explicit | User must select a model per agent |
| Tier degradation walk (current) | Works without user selection | Silent degradation, unpredictable cost |
| Runtime LLM picks model | Adaptive | Extra token cost, non-deterministic |
