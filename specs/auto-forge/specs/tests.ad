# Tests

---

## Specs-T1 [G8] useGateInbox 暂停 — 暂停的关卡在超时后重新出现
**Status:** draft

**状态：** 已完成
**测试文件：** `packages/auto-forge-ui/tests/unit/useGateInbox.spec.ts`
**标签：** stack:frontend
**依赖项：** Chat-G1, Relay-D6

```typescript
const inbox = useGateInbox()
inbox.registerGate(gateA)
```

1. 调用 `inbox.snoozeGate(gateA.id, 0.1)`（测试用 100ms）
2. 断言活跃秘书为 null
3. 等待 150ms
4. 断言 gateA 重新变为活跃




---

---

---

---

---

---

---
---

## Specs-T2 [G9] 类别渲染器按 section_type 正确切换
**Status:** draft

**状态：** 已完成
**测试文件：** `packages/auto-forge-ui/tests/integration/CategoryRenderers.spec.ts`
**标签：** stack:frontend
**依赖项：** Specs-G2, Specs-D1

1. 渲染 `<SpecsView />`
2. 点击每个侧边栏章节
3. 断言正确挂载渲染器组件
4. 断言 `GoalsTable` 渲染基于 div 的树；`ArchitectureCards` 渲染卡片




---

---

---

---

---

---

---
---

## Specs-T3 [G9] RelationsPanel 跨章节解析父项和子项
**Status:** draft

**状态：** 已完成
**测试文件：** `packages/auto-forge-ui/tests/integration/RelationsPanel.spec.ts`
**标签：** stack:frontend
**依赖项：** Specs-G2, Specs-D2

1. 在 GoalsTable 中选择 AgentConfig-G1
2. 断言 RelationsPanel 显示 "子项：Relay-A1, AgentConfig-D1, AgentConfig-T1"
3. 点击 Relay-A1；断言章节切换为架构，Relay-A1 展开
4. 断言 Relay-A1 RelationsPanel 显示 "父项：AgentConfig-G1"




---

---

---

---

---

---

---
---

## Specs-T4 [G21] GoalsTable 渲染带切换交互的层级树
**Status:** draft

**状态：** 草案
**测试文件：** `frontend/src/components/category/GoalsTable.vue`（组件测试）
**标签：** stack:frontend
**依赖项：** Specs-G7, Specs-D4

1. 渲染 `GoalsTable`，父目标为 `Chat-G1`，子目标为 `Chat-G2`
2. 断言 `Chat-G1` 行有 `.node-toggle-area`（有子项）
3. 断言 `Chat-G2` 行有 `.node-indent` 但无 `.node-toggle-area`
4. 点击 `Chat-G1` 上的切换区域 → 断言 `Chat-G2` 行出现在 DOM 中
5. 再次点击切换区域 → 断言 `Chat-G2` 行从 DOM 中移除
6. 点击 `Chat-G1` 行（切换区域外）→ 断言 `@open-detail` 以 `Chat-G1` 触发

---

---

---

---

---

---

---

---
---

## Specs-T5 [G21] GoalDetailModal 正确打开、编辑和关闭
**Status:** draft

**状态：** 草案
**测试文件：** `frontend/src/components/GoalDetailModal.vue`（组件测试）
**标签：** stack:frontend
**依赖项：** Specs-G7, Specs-D5

1. 渲染 `GoalDetailModal`，带一个目标条目
2. 断言弹窗遮罩和卡片存在
3. 断言头部显示 ID 徽章和标题
4. 断言元数据栏显示优先级、状态和标签胶囊
5. 断言 RelationsPanel 在主体中渲染
6. 点击 "编辑" 按钮 → 断言 `@edit` 触发，`GoalEditor` 挂载
7. 按 ESC → 断言 `@close` 触发
8. 点击遮罩背景 → 断言 `@close` 触发
9. 点击弹窗卡片 → 断言未触发 `@close`（click.stop）

---

---

---

---

---

---

---

---
---

## Specs-T6 [G21] 侧边栏 Stack/Module 筛选器应用 AND 逻辑
**Status:** draft

**状态：** 草案
**测试文件：** `frontend/src/views/SpecsView.vue`（组件测试）
**标签：** stack:frontend
**依赖项：** Specs-G7, Specs-D6

1. 渲染 `SpecsView`，带混合标签条目（stack:frontend+module:specs, stack:backend+module:relay）
2. 在 Stack 抽屉中选择 "frontend" → 断言仅显示 frontend 条目
3. 同时在 Module 抽屉中选择 "specs" → 断言仅显示 frontend+specs 条目
4. 取消所有 Module 标签 → 断言显示所有 frontend 条目（仅 stack 筛选器）
5. 取消所有 Stack 标签 → 断言显示所有条目（无筛选器）
6. 选择不匹配的组合 → 断言显示空状态

---

---

---

---

---

---

---

---
---

## Specs-T7 [G21] 标签徽章显示缩短的颜色编码值
**Status:** draft

**状态：** 草案
**测试文件：** `frontend/src/components/category/GoalsTable.vue`（组件测试）
**标签：** stack:frontend
**依赖项：** Specs-G7, Specs-D6

1. 渲染 `GoalsTable`，条目标签为 `stack:frontend, module:specs`
2. 断言标签芯片显示文本 "frontend" 和 "specs"（非完整 `kind:value`）
3. 断言 `stack` 芯片有 `.tag-stack` 类（蓝色样式）
4. 断言 `module` 芯片有 `.tag-module` 类（紫色样式）
5. 悬停芯片 → 断言 `title` 属性包含完整标签文本 `stack:frontend`
6. 添加 3+ 标签 → 断言仅前 2 个内联渲染，溢出显示 `+N`
