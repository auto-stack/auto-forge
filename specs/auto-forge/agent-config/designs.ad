# Designs

---

## AgentConfig-D1 AgentInstance 与人格渲染
**Status:** draft

**状态：** 已实现
**模块：** `crates/auto-forge/src/relay/agent.rs`
**标签：** stack:backend
**依赖于：** AgentConfig-A1

```rust
pub struct AgentInstance {
    pub id: String,
    pub profession: Profession,
    pub soul: SoulConfig,
    pub model: ModelConfig,
    pub context: AgentContext,
}

impl AgentInstance {
    /// Spawn a new agent for a pipeline step.
    /// Loads Soul from disk, resolves model config (with fallback chain).
    pub fn spawn(
        profession: &Profession,
        soul_id: &str,
        handoff: &HandoffDocument,
        specs: &SpecsDocument,
    ) -> Result<Self, AgentError>;

    /// Render the system prompt from Soul + Profession + context.
    pub fn render_system_prompt(&self) -> String;

    /// Render the user message from Handoff + relevant Specs.
    pub fn render_user_message(&self) -> Vec<ChatMessage>;

    /// Execute one turn of the ReAct loop.
    pub async fn turn(&mut self, tools: &[Box<dyn Tool>]) -> TurnResult;
}

pub struct SoulConfig {
    pub id: String,
    pub name: String,
    pub markdown: String,
    pub values: Vec<String>,
    pub handoff_ritual: String,
}

pub struct ModelConfig {
    pub provider: Provider,
    pub model: String,
    pub temperature: f32,
    pub max_tokens: u32,
    pub reasoning_budget: Option<u32>,
    pub fallback_chain: Vec<String>,
}

pub enum Provider {
    Anthropic,
    OpenAI,
    Local { url: String },
}
```

```mermaid
stateDiagram-v2
    [*] --> Unresolved: Soul ID provided
    Unresolved --> Resolved: Soul loaded from disk
    Resolved --> Ready: Model config validated
    Ready --> Thinking: Turn started
    Thinking --> ToolCall: LLM requests tool
    ToolCall --> Thinking: Tool result returned
    Thinking --> Complete: LLM finishes
    Thinking --> Error: API failure
    Error --> Ready: Fallback model activated
    Error --> Failed: Fallback exhausted
    Complete --> [*]
    Failed --> [*]
```

| 字段 | 类型 | 描述 |
|---|---|---|
| `id` | `String` | 此智能体实例的 UUID |
| `profession.id` | `String` | 对职业注册表的引用 |
| `soul.id` | `String` | 对人格配置文件的引用 |
| `model.provider` | `Provider` | API 提供商枚举 |
| `model.fallback_chain` | `Vec<String>` | 替代模型的有序列表 |
| `context.budget_used` | `u64` | 此步骤消耗的 Token 数 |
| `context.turns_taken` | `u32` | 此步骤的 LLM 轮次数 |


---

---

---

---

---

---

---

---

---

## AgentConfig-D2 职业注册表（v2）
**Status:** draft

**状态：** 已实现
**模块：** `crates/auto-forge/src/relay/profession.rs`
**标签：** stack:backend
**依赖于：** AgentConfig-G1、Specs-G1

```rust
pub struct Profession {
    pub id: String,
    pub name: String,
    pub phase: ForgePhase,
    pub owned_sections: Vec<SectionType>,
    pub readable_sections: Vec<SectionType>,
    pub allowed_tools: Vec<String>,
    pub handoff_to: Vec<String>,
    pub approval_gates: Vec<String>,
    pub max_turns: u32,
    pub token_budget: u64,
}
```


| 职业 | 阶段 | 拥有的规格 | 模型层级 | 关卡 |
|---|---|---|---|---|
| Assistant | Intake | None | Light | Auto |
| Advisor | Discovery | Goals | Medium | Human (GSD) / Human (Check) |
| Architect | Design | Architecture, Designs | Heavy | Auto (GSD) / Human (Check) |
| Planner | Planning | Plans | Medium | Auto |
| Coder | Execution | None (code) | Heavy | Auto |
| Tester | Execution | Tests | Medium | Auto |
| Reviewer | Verification | Reviews | Heavy | Auto |
| Documenter | Report | Reports | Light | Auto |

| 字段 | 类型 | 描述 |
|---|---|---|
| `id` | `String` | 职业标识符 |
| `phase` | `ForgePhase` | 该职业运行的阶段 |
| `owned_sections` | `Vec<SectionType>` | 该职业可以编写的规格 |
| `readable_sections` | `Vec<SectionType>` | 该职业可以读取的规格 |
| `approval_gates` | `Vec<String>` | 需要审批的交接目标 |


---

---

---

---

---

---

---

---

---

---

## AgentConfig-D3 架构师人格 v2 —— 代码验证与质量约束
**Status:** draft

**状态：** 已实现
**模块：** `backend/src/relay/souls/architect.md`
**标签：** stack:backend
**依赖于：** AgentConfig-D1、AgentConfig-D2、Specs-D8

### 问题

原始架构师人格要求读取现有规格并编写架构/设计，但缺乏对以下方面的约束：
- **准确性**：架构师可以未经核实就声称技术栈或文件路径
- **深度**：架构项有结构图但没有运行时序列图
- **质量**：交接前没有自检清单，导致交付物不完整

### 解决方案

通过三个新的约束块增强架构师人格：**代码验证强制要求**、**深度要求**和**质量检查清单**。

#### 1. 代码验证强制要求

在编写任何引用源代码的架构或设计规格之前：

| 规则 | 执行方式 |
|------|----------|
| 验证技术栈 | 派遣 gofer 在声称依赖之前读取 `Cargo.toml` 和 `package.json` |
| 验证文件存在性 | 派遣 gofer 在引用之前确认文件路径和行号 |
| 引用代码证据 | 每个结构结论必须引用实际代码：`backend/src/forge/mod.rs:2024-2076` |

#### 2. 深度要求

对于架构中描述的每个核心机制：

| 要求 | 格式 |
|------|------|
| 序列图 | Mermaid `sequenceDiagram` 展示运行时行为 |
| 触发条件 | "当 X 发生时，此机制执行" |
| 数据流 | `[input]` → `[processing]` → `[output]` |
| 设计亮点 | 1-2 句话解释"为什么选择此设计" |

#### 3. 质量检查清单（交接前自检）

```markdown
- [ ] All tech stack claims verified against config files
- [ ] All file paths and line numbers confirmed by gofer
- [ ] Every Architecture item has a Mermaid structural diagram
- [ ] Every core mechanism has a Mermaid sequence diagram
- [ ] Every decision has a Trade-offs table with ≥2 alternatives
- [ ] All class/function/struct names match actual code (not imagined)
- [ ] Every design includes an interface, state machine, and data model
- [ ] No unhandled error cases
- [ ] Explicit data lifecycle definitions
```

### 对职业配置的影响

架构师职业（`backend/src/relay/profession.rs`）已经具有：
- `owned_sections: [Architecture, Designs]`
- `allowed_tools: [read_specs, write_specs, update_spec, list_specs, read_file, write_file, query_wiki, list_wiki, bring_in, spawn_relay]`

无需更改工具——人格约束通过提示工程而非工具门控来执行。`dispatch` 工具（已通过 `bring_in` / 差事机制可用）用于生成 gofer 以进行代码验证。

### 相关技能增强

`forge-write-architecture` 技能同时增强以要求：
- ADR 模板中的 `Sequence Diagram` 部分
- 包含文件路径 + 行范围的 `Code Evidence` 部分
- 运行时行为的 `Data Flow` 部分
- 解释依据的 `Design Highlight` 部分

### 要求

- 架构师必须在声称框架之前验证技术栈
- 架构师必须在引用行号之前验证文件路径
- 架构师必须为运行时机制包含序列图
- 架构师必须在交接前完成质量检查清单
- 质量违规被视为交接阻塞项

---
