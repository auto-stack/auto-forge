# Reports

---

## Relay-R2 — 聊天 → 接力运行端到端流水线修复
**Status:** draft

**状态：** 已完成
**标签：** stack:backend
**日期：** 2026-05-30

聊天 → spawn_relay → 接力运行自动化流水线的实现后报告。端到端测试期间发现八个错误，均已修复并验证。

### 应用的修复

| # | 问题 | 文件 | 修复 |
|---|-------|------|-----|
| 1 | `forge_poll_run_phase` 返回错误的职业 | `src/mcp/mod.rs` | `saturating_sub(1)` → `run.current_step` |
| 2 | `forge_batch_get_results` 返回错误的职业 | `src/mcp/mod.rs` | 相同的基于 0 的索引修复 |
| 3 | 聊天提示缺少工具使用指令 | `src/forge/mod.rs` | 追加 "actively use available tools" |
| 4 | 聊天提示缺少接力运行触发器 | `src/forge/mod.rs` | 追加 "call `spawn_relay` tool" 指令 |
| 5 | `relay_update` 事件缺失 | `src/relay/driver.rs` | 在 `step_started` / `step_completed` 后广播 |
| 6 | `step_completed` 记录了错误的 step_id | `src/relay/store.rs` | 在 `engine.submit_handoff()` 前捕获 `completed_step_id` |
| 7 | 编码员步骤循环/卡住 | `src/relay/profession.rs` | `max_turns`：12 → 30 |
| 8 | Token 跟踪丢失 | `src/relay/driver.rs` | `TurnEvent::Usage` 处理器推送每轮次 `TokenSpend` |

### 额外修复（验证阶段）

| # | 问题 | 文件 | 修复 |
|---|-------|------|-----|
| 9 | `/api/health` 处理器未注册 | `src/forge/mod.rs` | 添加 `.route("/api/health", get(health))` |
| 10 | 每轮次 Token 跟踪不精确 | `src/relay/turn.rs` + `driver.rs` | 每次内部聊天轮次后发出 `TurnEvent::Usage` |
| 11 | `relay_update` 未持久化 | `src/relay/store.rs` + `driver.rs` | 添加 `RunEvent::RelayUpdate` 变体 |
| 12 | 重复的 `spawn_relay` 调用 | `src/forge/mod.rs` | `spawn_relay_done` 标志中断 ReAct 循环 |
| 13 | Token 3 倍重复 | `src/relay/driver.rs` + `store.rs` | 单一来源：仅 `TurnEvent::Usage` 推送 `TokenSpend` |
| 14 | 人工关卡阻塞发现步骤 | `src/relay/flows.rs` | `GateType::Human` → `Auto` 用于发现步骤 |

### 验证结果

- **运行自动推进：** `intake` → `discover` → `design` → `plan` → `draft-tests`，无需 `waiting_approval`
- **Token 事件：** 38 个 `token_spend` 事件，0 个重复
- **步骤完成：** 每步恰好 1 个 `step_completed` 事件
- **测试：** `cargo test --lib` 173/173 通过，接力模块 87/87 通过

### 已知限制

1. **聊天 MCP 工具缺少工具支持：** `forge_send_message` 使用非流式 `ai_provider.chat()`。仅 Web UI `forge_stream` 支持工具调用。
2. **编码员探索循环：** 即使 `max_turns=30`，编码员仍可能在探索上花费大量 Token 预算。考虑添加步骤级硬预算上限。
3. **代码步骤双重执行：** 当交接验证器失败时，`fast-track` 流程偶尔执行代码步骤两次（迭代 0 + 1）。

---

## Relay-R1 — Forge 规范接力前端实现
**Status:** draft

**状态：** 草案
**标签：** stack:frontend

重新设计的规范接力前端的实现，涵盖聊天优先关卡（智能秘书）、实时节点图流水线、带双向可追溯性的规范工作区和响应式打磨。所有 P0 差距已关闭。构建通过。开发服务器和后端 API 运行中。

| 指标 | 值 |
|---|---|
| 目标达成 | 4/4 (Chat-G1–UiSystem-G1) |
| 测试通过 | 14/14 (S8.1–S10.4 + S7.2) |
| 计划完成 | 5/5 (Chat-P1–UiSystem-P1) |
| 设计实现 | 11/11 (Chat-D1–Relay-D10) |
| 代码文件变更 | ~30 个 Vue 组件 + 7 个组合式函数 + 2 个类型文件 |

| 职业 | Token | 成本 |
|---|---|---|
| 顾问 | 4,200 | $0.13 |
| 架构师 | 12,500 | $0.38 |
| 计划员 | 3,800 | $0.11 |
| 编码员 | 58,000 | $1.74 |
| 测试员 | 24,000 | $0.72 |
| 评审员 | 6,200 | $0.19 |
| **总计** | **108,700** | **$3.27** |


---
