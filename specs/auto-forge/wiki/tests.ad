# Tests

---

## Wiki-T1 [G18] Wiki RAG 语义检索
**Status:** draft

**状态：** 提议中
**测试文件：** `backend/tests/wiki_rag.rs`
**标签：** stack:both
**依赖项：** Wiki-G1, Wiki-D1

1. 创建 5 个包含不同技术内容的 wiki 页面
2. 调用 `query_wiki("timer register configuration")` 工具
3. 断言前 3 个结果语义相关（timer/register 页面排名更高）
4. 断言每个结果 ≤500 tokens
5. 验证空查询返回错误，而非所有页面

- 向量相似性搜索返回相关段落。
- 每次检索的 token 预算受限制。
- 空/异常查询返回错误。

---

---

---

---

---

---

---
---

## Wiki-T2 [G18] Wiki 工具职业访问控制
**Status:** draft

**状态：** 完成
**测试文件：** `backend/tests/wiki_tools.rs`
**标签：** stack:both
**依赖项：** Wiki-G1, Wiki-D1

1. 断言所有职业均可调用 `query_wiki` 和 `list_wiki`
2. 断言仅 Coder 和 Documenter 可调用 `create_wiki_page` 和 `update_wiki_page`
3. 验证具有错误职业的智能体在写入工具上收到 PermissionDenied
4. 验证具有正确职业的智能体在写入工具上成功

- 读取工具（query、list）对所有职业可用。
- 写入工具（create、update）限制为 Coder 和 Documenter。

---

---

---

---

---

---

---
---

## Wiki-T3 [G18] Wiki 嵌入流水线
**Status:** draft

**状态：** 提议中
**测试文件：** `backend/tests/wiki_embedding.rs`
**标签：** stack:backend
**依赖项：** Wiki-G1, Wiki-D1

1. 创建包含技术内容的 wiki 页面
2. 断言嵌入向量已计算并存储
3. 更新页面内容 —— 断言嵌入重新计算
4. 验证嵌入维度与模型输出匹配
5. 验证页面操作后 FAISS 索引已更新

- 页面创建/更新时计算嵌入。
- FAISS 索引与页面内容保持同步。
- 嵌入维度一致。

---

---

---

---

---

---

---
---

## Wiki-T4 [G18] Wiki 标签页在导航栏中以 BookOpen 图标渲染
**Status:** draft

**状态：** 完成
**测试文件：** `frontend/src/App.vue`（组件测试）
**标签：** stack:frontend
**依赖项：** Wiki-G1, Wiki-D2

1. 渲染 `<App />`
2. 断言导航栏包含带有 BookOpen 图标的 "Wiki" 标签页
3. 点击 Wiki 标签页
4. 断言 `currentView === 'wiki'`
5. 断言 `<WikiView>` 组件已挂载

- Wiki 标签页出现在导航栏中 Specs 之后、Relay 之前。
- 点击切换视图到 WikiView。

---

---

---

---

---

---

---
---

## Wiki-T5 [G18] WikiView 侧边栏加载双区域树（Raw + Wiki）并支持搜索过滤
**Status:** draft

**状态：** 完成
**测试文件：** `frontend/src/views/WikiView.vue`（组件测试）
**标签：** stack:frontend
**依赖项：** Wiki-G1, Wiki-D2

1. 使用返回 3 个页面的模拟 API 渲染 `<WikiView />`
2. 断言侧边栏显示 3 个页面项（标题 + 标签）
3. 输入仅匹配 1 个页面标题的搜索查询
4. 断言侧边栏过滤为 1 个项

- 页面列表从 `GET /api/forge/wiki/{project}/pages` 加载。
- 搜索输入框按标题和标签在客户端过滤页面。

---

---

---

---

---

---

---
---

## Wiki-T6 [G18] 点击 wiki 页面加载完整内容并渲染 markdown
**Status:** draft

**状态：** 完成
**测试文件：** `frontend/src/views/WikiView.vue`（组件测试）
**标签：** stack:frontend
**依赖项：** Wiki-G1, Wiki-D2

1. 使用模拟的 `loadPage` 渲染 `<WikiView />`
2. 点击侧边栏中的第一个页面
3. 断言 `loadPage(project, slug)` 被正确 slug 调用
4. 断言 `MarkdownContent` 组件渲染页面内容
5. 断言内容头部显示页面标题、来源类型徽标、版本

- 完整页面内容从 `GET /api/forge/wiki/{project}/page/{slug}` 加载。
- Markdown 通过现有的 `MarkdownContent.vue` 渲染。

---

---

---

---

---

---

---
---

## Wiki-T7 [G18] 编辑模式切换为 MarkdownEditor 并保存更新
**Status:** draft

**状态：** 完成
**测试文件：** `frontend/src/views/WikiView.vue`（组件测试）
**标签：** stack:both
**依赖项：** Wiki-G1, Wiki-D2

1. 渲染 `<WikiView />`，选择一个页面
2. 点击 "Edit" 按钮
3. 断言 `MarkdownEditor` 组件替换 `MarkdownContent`
4. 在编辑器中修改内容
5. 点击 "Save"
6. 断言 `updatePage(project, slug, data)` 被更新后的内容调用
7. 断言视图返回到带更新内容的 `MarkdownContent`

- 编辑按钮在查看和编辑模式之间切换。
- 保存调用 `PUT /api/forge/wiki/{project}/page/{slug}` 并重新加载。

---

---

---

---

---

---

---
---

## Wiki-T8 [G18] 创建新页面流程使用基于路径的 slug 创建页面并添加到树中
**Status:** draft

**状态：** 完成
**测试文件：** `frontend/src/views/WikiView.vue`（组件测试）
**标签：** stack:both
**依赖项：** Wiki-G1, Wiki-D2

1. 渲染 `<WikiView />`
2. 点击侧边栏头部的 "+" 按钮
3. 在内联表单中填写 slug、标题、内容
4. 点击 "Create"
5. 断言 `createPage(project, page)` 被正确数据调用
6. 断言新页面出现在侧边栏中
7. 断言新创建的页面被选中并渲染

- 创建流程调用 `POST /api/forge/wiki/{project}/pages`。
- 新页面创建后立即出现在侧边栏中。

---

---

---

---

---

---

---
---

## Wiki-T9 [G18] 删除页面在确认后从树中移除
**Status:** draft

**状态：** 完成
**测试文件：** `frontend/src/views/WikiView.vue`（组件测试）
**标签：** stack:frontend
**依赖项：** Wiki-G1, Wiki-D2

1. 渲染 `<WikiView />`，选择一个页面
2. 点击 "Delete" 按钮
3. 断言确认对话框出现
4. 确认删除
5. 断言 `deletePage(project, slug)` 被调用
6. 断言页面从侧边栏移除
7. 断言显示空状态（未选择页面）

- 删除调用 `DELETE /api/forge/wiki/{project}/page/{slug}`。
- 确认对话框防止意外删除。

---

---

---

---

---

---

---
---

## Wiki-T10 [G18] Wiki 侧边栏折叠状态持久化到 localStorage
**Status:** draft

**状态：** 完成
**测试文件：** `frontend/src/views/WikiView.vue`（组件测试）
**标签：** stack:frontend
**依赖项：** Wiki-G1, Wiki-D2

1. 渲染 `<WikiView />`
2. 点击侧边栏折叠按钮
3. 断言侧边栏宽度缩减为仅图标模式
4. 断言 `localStorage.getItem('autoforge-wiki-sidebar-collapsed') === 'true'`
5. 重新加载组件
6. 断言侧边栏以折叠状态启动（从 localStorage 读取）

- 折叠状态在页面重载间持久化。
- 键：`autoforge-wiki-sidebar-collapsed`。

---

---

---

---

---

---

---
