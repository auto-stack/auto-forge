# Tests

---

## Cli-T1 [G17] BringInTool 根据 handoff_to 列表验证目标
**Status:** draft

**状态：** 提议中
**测试文件：** `backend/src/forge/tools.rs`（内联 `#[cfg(test)]`）
**标签：** stack:backend
**依赖项：** Relay-G9, Chat-D3

```rust
let tool = BringInTool;
// 有效：advisor 在 assistant 的 handoff_to 中
let valid = tool.execute(json!({"target": "advisor", "reason": "test"}));
// 无效：architect 不在 assistant 的 handoff_to 中
let invalid = tool.execute(json!({"target": "architect", "reason": "test"}));
```

1. 以 assistant 作为活跃职业调用 `bring_in(target: "advisor")`
2. 断言结果为 Ok，附带 "Handed off to Isaac (advisor)"
3. 以 assistant 作为活跃职业调用 `bring_in(target: "architect")`
4. 断言结果为 Err，附带 "Cannot hand off to 'architect'"
5. 以 advisor 已是活跃职业调用 `bring_in(target: "advisor")`
6. 断言结果为 Err，附带 "Already talking to advisor"

- 有效目标成功。
- 不在 `handoff_to` 中的目标被拒绝。
- 与当前职业相同的目标被拒绝。

---

---

---

---

---

---

---
---
